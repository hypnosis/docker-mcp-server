# 🧪 Testing System Overview

## Система тестирования Docker MCP Server

Полная система тестирования, покрывающая все 20 MCP команд через автоматические и ручные тесты.

---

## 📁 Структура файлов

```
docker-mcp-server/
├── docker-compose.test.yml          # Тестовое окружение (web, postgres, redis)
├── test-data/                       # Данные для тестов
│   ├── postgres/init.sql           # Инициализация PostgreSQL
│   ├── web/package.json            # Тестовое Node.js приложение
│   └── web/index.js                # Простой web сервер
├── tests/
│   ├── e2e/mcp-tools.test.ts       # E2E тесты всех MCP tools
│   ├── unit/                       # Unit тесты
│   └── integration/                # Integration тесты
├── pre-commit.sh                    # Скрипт для запуска перед коммитом
├── MANUAL_TEST.md                   # Чек-лист для ручного тестирования
└── package.json                     # Команды для тестирования
```

---

## 🚀 Quick Start

### 1. Запустить тестовое окружение

```bash
# Запуск
npm run docker:test:up

# Ожидание готовности (30 секунд)
sleep 30

# Проверка статуса
docker-compose -f docker-compose.test.yml ps
```

### 2. Запустить все тесты

```bash
# Полный цикл тестирования (один шаг!)
./pre-commit.sh
```

Или отдельные части:

```bash
# Только unit тесты
npm run test

# Только E2E тесты
npm run test:e2e

# С покрытием
npm run test:coverage

# Все автоматические тесты
npm run test:all
```

### 3. Ручное тестирование через AI

Следуй инструкциям в [MANUAL_TEST.md](./MANUAL_TEST.md)

---

## 🎯 Что покрывают тесты

### Автоматические тесты (tests/e2e/mcp-tools.test.ts)

Покрывают все 20 MCP команд:

#### Container Tools (9)
- ✅ `docker_container_list()` — список всех контейнеров
- ✅ `docker_container_list({project})` — список по проекту
- ✅ `docker_container_start()` — запуск контейнера
- ✅ `docker_container_stop()` — остановка контейнера
- ✅ `docker_container_restart()` — перезапуск контейнера
- ✅ `docker_container_logs()` — логи контейнера
- ✅ `docker_container_stats()` — статистика контейнера
- ✅ `docker_resource_list({type: "images"})` — список образов
- ✅ `docker_resource_list({type: "volumes"})` — список volumes

#### Database Tools (4)
- ✅ `docker_db_status({service: "postgres"})` — статус PostgreSQL
- ✅ `docker_db_status({service: "redis"})` — статус Redis
- ✅ `docker_db_query({service: "postgres", query})` — SQL запросы
- ✅ `docker_db_query({service: "redis", query})` — Redis команды
- ✅ `docker_db_backup()` — создание бэкапа

#### Executor Tool (1)
- ✅ `docker_exec()` — выполнение команд в контейнере

#### Env Tools (3)
- ✅ `docker_env_list()` — список переменных окружения
- ✅ `docker_compose_config()` — конфигурация docker-compose
- ✅ `docker_healthcheck()` — проверка здоровья сервисов

#### Utility Tools (2)
- ✅ `docker_mcp_health()` — статус MCP сервера
- ✅ `docker_profile_info()` — информация о профилях

#### Discovery Tools (1)
- ✅ `docker_projects()` — список Docker проектов

### Ручные тесты (MANUAL_TEST.md)

- ✅ Тестирование как конечный пользователь (AI ассистент)
- ✅ Критические сценарии использования
- ✅ Тестирование LOCAL и REMOTE профилей
- ✅ Проверка edge cases

---

## 📋 Команды тестирования

### Основные команды

```bash
# 🚀 ГЛАВНАЯ КОМАНДА - запуск перед коммитом
./pre-commit.sh

# Сборка проекта
npm run build

# Unit тесты
npm run test
npm run test:watch         # С автообновлением
npm run test:ui            # С UI интерфейсом

# E2E тесты
npm run test:e2e
npm run test:e2e:watch     # С автообновлением

# Все тесты
npm run test:all

# Покрытие кода
npm run test:coverage      # Результат в coverage/index.html
```

### Docker команды

```bash
# Управление тестовым окружением
npm run docker:test:up      # Запустить
npm run docker:test:down    # Остановить
npm run docker:test:logs    # Показать логи
npm run docker:test:clean   # Очистить (удалить volumes)
```

---

## 🔄 Workflow перед коммитом

### Автоматический (рекомендуется)

```bash
./pre-commit.sh
```

Скрипт выполняет:
1. ✅ Clean & Build
2. ✅ TypeScript проверка
3. ✅ Unit тесты
4. ✅ Coverage генерация
5. ✅ Docker окружение проверка
6. ✅ E2E тесты
7. ✅ Напоминание о ручных тестах
8. ✅ Финальные проверки

### Ручной

```bash
# 1. Сборка
npm run clean
npm run build

# 2. Тесты
npm run test
npm run test:coverage

# 3. Docker окружение
npm run docker:test:up
sleep 30

# 4. E2E тесты
npm run test:e2e

# 5. Ручное тестирование
# Следуй MANUAL_TEST.md

# 6. Готово!
git add .
git commit -m "your message"
```

---

## 🐛 Troubleshooting

### Проблема: Docker контейнеры не запускаются

```bash
# Полная очистка и перезапуск
npm run docker:test:clean
npm run docker:test:up
sleep 30
docker-compose -f docker-compose.test.yml ps
```

### Проблема: PostgreSQL не инициализируется

```bash
# Удалить volume и пересоздать
docker volume rm docker-mcp-server_postgres_test_data
npm run docker:test:up
sleep 30

# Проверить инициализацию
docker-compose -f docker-compose.test.yml logs postgres | grep "init.sql"
```

### Проблема: E2E тесты падают с timeout

Увеличь timeout в `tests/e2e/mcp-tools.test.ts`:

```typescript
const DOCKER_TIMEOUT = 60000; // было 30000
```

### Проблема: Unit тесты проходят, E2E падают

```bash
# Проверь что тестовое окружение запущено
docker-compose -f docker-compose.test.yml ps

# Проверь логи
npm run docker:test:logs

# Проверь healthchecks
docker inspect test-postgres | grep -A 5 Health
```

### Проблема: Coverage < 80%

Это нормально для некоторых файлов:
- `src/index.ts` — entry point, сложно тестировать
- `src/cli.ts` — CLI интерфейс
- Mock файлы в tests/

Фокусируйся на покрытии бизнес-логики:
- Tools (>90%)
- Managers (>90%)
- Utils (>85%)
- Adapters (>80%)

---

## 📊 Метрики качества

### Целевые показатели

- ✅ Unit tests coverage: **> 80%**
- ✅ E2E tests: **20/20 команд покрыто**
- ✅ TypeScript errors: **0**
- ✅ Build errors: **0**
- ✅ Critical scenarios: **5/5 работают**

### Текущий статус

Запусти для проверки:

```bash
# Покрытие
npm run test:coverage

# Открыть отчет
open coverage/index.html

# Статус тестов
npm run test:all
```

---

## 🎯 Критерии готовности к коммиту

### Обязательно (Blocker)

- [ ] `./pre-commit.sh` завершился успешно
- [ ] Все E2E тесты прошли
- [ ] Все Unit тесты прошли
- [ ] TypeScript компилируется без ошибок
- [ ] Docker тестовое окружение запускается

### Рекомендуется

- [ ] Coverage > 80%
- [ ] Все 5 критических сценариев протестированы вручную
- [ ] Документация обновлена
- [ ] CHANGELOG.md содержит изменения

### Опционально

- [ ] REMOTE профиль протестирован (если настроен)
- [ ] Performance тесты пройдены
- [ ] Load тесты пройдены

---

## 🚀 CI/CD Integration

### GitHub Actions (пример)

```yaml
# .github/workflows/test.yml
name: Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Start test environment
        run: |
          npm run docker:test:up
          sleep 30
      
      - name: Run tests
        run: ./pre-commit.sh
      
      - name: Upload coverage
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage/coverage-final.json
```

---

## 📚 Дополнительные ресурсы

- [MANUAL_TEST.md](./MANUAL_TEST.md) — Детальный чек-лист для ручного тестирования
- [tests/e2e/mcp-tools.test.ts](./tests/e2e/mcp-tools.test.ts) — E2E тесты
- [docker-compose.test.yml](./docker-compose.test.yml) — Тестовое окружение
- [pre-commit.sh](./pre-commit.sh) — Скрипт пре-коммита

---

## 🎓 Best Practices

### Перед началом работы

1. Запусти тестовое окружение: `npm run docker:test:up`
2. Убедись что все healthy: `docker-compose -f docker-compose.test.yml ps`
3. Запусти тесты в watch режиме: `npm run test:watch`

### Во время разработки

1. Пиши unit тесты для новой логики
2. Запускай E2E тесты после больших изменений
3. Следи за coverage (должен расти, не падать)

### Перед коммитом

1. **ВСЕГДА** запускай `./pre-commit.sh`
2. Проверь ручной чек-лист в [MANUAL_TEST.md](./MANUAL_TEST.md)
3. Обнови CHANGELOG.md
4. Обнови документацию (если нужно)

### После коммита

1. Убедись что CI/CD прошел (если настроен)
2. Проверь что версия в package.json корректна
3. Тегай релизы при публикации

---

## 🔒 Security Testing

### SQL Injection тесты

E2E тесты включают проверку:
- ✅ SQL валидация работает
- ✅ Dangerous queries блокируются
- ✅ Prepared statements используются

### Secret Masking тесты

E2E тесты включают проверку:
- ✅ Секреты маскируются в `docker_env_list`
- ✅ Пароли не логируются
- ✅ Приватные ключи не отображаются

---

## 📈 Future Improvements

- [ ] Performance benchmarks
- [ ] Load testing для множественных запросов
- [ ] Memory leak detection
- [ ] Security scanning (Snyk, npm audit)
- [ ] Visual regression testing
- [ ] Chaos engineering tests

---

**Последнее обновление:** 2026-01-09  
**Версия:** 1.0.0  
**Автор:** Danila Susak
