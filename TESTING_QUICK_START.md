# ⚡ Testing Quick Start

## Один шаг перед коммитом

```bash
./pre-commit.sh
```

**Готово!** Это запустит все тесты и проверки.

---

## Что происходит?

```
╔════════════════════════════════════════════════════════════════╗
║         Docker MCP Server - Pre-Commit Tests                  ║
╚════════════════════════════════════════════════════════════════╝

▶ Step 1: Clean and Build
  ✓ Clean completed
  ✓ Build successful

▶ Step 2: TypeScript Check
  ✓ TypeScript check passed

▶ Step 3: Unit Tests
  ✓ Unit tests passed

▶ Step 4: Test Coverage
  ✓ Coverage generated

▶ Step 5: Docker Environment Check
  ✓ Docker is running
  ✓ Test environment is running

▶ Step 6: E2E Tests (All MCP Tools)
  ✓ E2E tests passed

▶ Step 7: Manual Test Reminder
  ⚠ REMINDER: Have you tested manually?

▶ Step 8: Final Checks
  ✓ Changes ready to commit
  ✓ Version check completed

╔════════════════════════════════════════════════════════════════╗
║          ALL TESTS PASSED! Ready to commit ✓                   ║
╚════════════════════════════════════════════════════════════════╝
```

---

## Первый запуск

### 1. Установи зависимости

```bash
npm install
```

### 2. Собери проект

```bash
npm run build
```

### 3. Запусти тестовое окружение

```bash
npm run docker:test:up
```

Подожди 30 секунд пока контейнеры запустятся и станут healthy.

### 4. Запусти тесты

```bash
./pre-commit.sh
```

---

## Структура тестов

```
📁 docker-mcp-server/
│
├── 🐳 docker-compose.test.yml          # Тестовое окружение
│   └── Services: web, postgres, redis
│
├── 📊 test-data/                       # Тестовые данные
│   ├── postgres/init.sql              # SQL с пользователями и постами
│   └── web/                           # Простое Node.js приложение
│
├── 🧪 tests/
│   ├── e2e/mcp-tools.test.ts          # E2E тесты всех 20 команд
│   ├── unit/                          # Unit тесты модулей
│   └── integration/                   # Integration тесты
│
├── 🚀 pre-commit.sh                    # Главный скрипт тестирования
├── 📖 MANUAL_TEST.md                   # Чек-лист для ручных тестов
├── 📚 TEST_SYSTEM.md                   # Полная документация
└── ⚡ README_TESTING.md                # Краткий гайд
```

---

## Что покрывают тесты?

### ✅ Все 20 MCP команд:

**Container Management (9):**
- List, start, stop, restart containers
- View logs and stats
- List images and volumes
- Compose up/down

**Database Operations (4):**
- Status check (PostgreSQL, Redis)
- Query execution (SQL, Redis)
- Backup creation

**Command Execution (1):**
- Execute any command in container

**Environment (3):**
- List env variables
- Show compose config
- Health checks

**Utilities (2):**
- MCP server health
- Profile information

**Discovery (1):**
- List Docker projects

**Profile Support (все команды):**
- LOCAL profile (default)
- REMOTE profiles (через profiles.json)

---

## Быстрое тестирование (5 минут)

### Запусти автоматические тесты:

```bash
npm run test:all
```

### Протестируй вручную через Cursor AI:

1. Открой Cursor
2. В чате напиши:

```
Используй Docker MCP Server:

1. Покажи все контейнеры
2. Выполни SELECT * FROM users в PostgreSQL
3. Выполни команду echo "test" в web контейнере
4. Проверь здоровье всех сервисов
```

3. Если все работает — **готово к коммиту!**

---

## Команды для ежедневной работы

```bash
# Запуск окружения
npm run docker:test:up

# Тесты во время разработки
npm run test:watch              # Unit тесты с автообновлением
npm run test:e2e:watch          # E2E тесты с автообновлением

# Перед коммитом
./pre-commit.sh

# Остановка окружения
npm run docker:test:down

# Полная очистка (включая volumes)
npm run docker:test:clean
```

---

## Troubleshooting

### ❌ Docker не запускается

```bash
npm run docker:test:clean
npm run docker:test:up
sleep 30
```

### ❌ Тесты падают

```bash
# Проверь статус
docker-compose -f docker-compose.test.yml ps

# Проверь логи
npm run docker:test:logs

# Перезапусти окружение
npm run docker:test:down
npm run docker:test:up
sleep 30
```

### ❌ PostgreSQL не инициализирован

```bash
docker volume rm docker-mcp-server_postgres_test_data
npm run docker:test:up
sleep 30
```

### ❌ E2E тесты timeout

Увеличь таймаут в `tests/e2e/mcp-tools.test.ts`:

```typescript
const DOCKER_TIMEOUT = 60000; // было 30000
```

---

## Критерии готовности

### ✅ Обязательно

- [ ] `./pre-commit.sh` завершился успешно
- [ ] Все тесты зеленые
- [ ] TypeScript без ошибок
- [ ] Docker окружение работает

### ⭐ Рекомендуется

- [ ] Coverage > 80%
- [ ] Ручное тестирование выполнено
- [ ] Документация обновлена
- [ ] CHANGELOG обновлен

---

## Детальная документация

| Документ | Описание |
|----------|----------|
| **[TEST_SYSTEM.md](./TEST_SYSTEM.md)** | Полная документация системы тестирования |
| **[MANUAL_TEST.md](./MANUAL_TEST.md)** | Детальный чек-лист ручных тестов |
| **[README_TESTING.md](./README_TESTING.md)** | Краткий гайд по тестированию |
| **[pre-commit.sh](./pre-commit.sh)** | Скрипт автоматического тестирования |

---

## CI/CD

После коммита убедись что CI/CD прошел (если настроен):

```bash
# Проверь GitHub Actions
gh run list

# Или открой в браузере
gh run view --web
```

---

## Быстрая справка

```bash
# Установка и сборка
npm install && npm run build

# Запуск тестового окружения
npm run docker:test:up && sleep 30

# Все тесты
./pre-commit.sh

# Коммит
git add .
git commit -m "your message"
git push
```

---

## 🎯 Итого

1. **Запусти:** `./pre-commit.sh`
2. **Дождись:** Все тесты прошли
3. **Коммить:** `git commit`

**Вот и всё!** 🚀

---

**Нужна помощь?**
- Открой [MANUAL_TEST.md](./MANUAL_TEST.md) для детального чек-листа
- Открой [TEST_SYSTEM.md](./TEST_SYSTEM.md) для полной документации
- Проверь [Troubleshooting](#troubleshooting) для решения проблем

---

**Последнее обновление:** 2026-01-09  
**Версия:** 1.0.0
