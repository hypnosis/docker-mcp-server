# 🧪 Quick Testing Guide

> **TL;DR:** Запусти `./pre-commit.sh` перед каждым коммитом!

---

## 🚀 One-Step Testing (Перед коммитом)

```bash
./pre-commit.sh
```

Этот скрипт делает **ВСЁ**:
- ✅ Сборка проекта
- ✅ TypeScript проверка
- ✅ Unit тесты
- ✅ Coverage генерация
- ✅ Docker окружение проверка
- ✅ E2E тесты всех 20 MCP команд
- ✅ Финальные проверки

**Время выполнения:** ~5-10 минут

---

## 📋 Что тестируется?

### Автоматически (E2E тесты)

**Container Tools (9 команд):**
- docker_container_list, start, stop, restart
- docker_container_logs, stats
- docker_resource_list (images, volumes)
- docker_compose_up, docker_compose_down

**Database Tools (4 команды):**
- docker_db_status (postgres, redis)
- docker_db_query (SQL, Redis)
- docker_db_backup

**Executor Tool (1 команда):**
- docker_exec

**Env Tools (3 команды):**
- docker_env_list
- docker_compose_config
- docker_healthcheck

**Utility Tools (2 команды):**
- docker_mcp_health
- docker_profile_info

**Discovery Tools (1 команда):**
- docker_projects

**Итого: 20 команд полностью покрыто**

---

## 🎯 Быстрое ручное тестирование (5 минут)

### 1. Запусти тестовое окружение

```bash
npm run docker:test:up
sleep 30
```

### 2. Протестируй через Cursor AI

В чате с AI попроси выполнить:

```
1. Покажи все контейнеры
2. Покажи пользователей из PostgreSQL (SELECT * FROM users)
3. Выполни echo test в web контейнере
4. Проверь здоровье всех сервисов
```

### 3. Готово!

Если все работает — можешь коммитить!

---

## 📦 Команды NPM

```bash
# Основная команда
./pre-commit.sh                # Полный цикл тестирования

# Сборка
npm run build                  # Билд проекта
npm run clean                  # Очистка dist/

# Тесты
npm run test                   # Unit тесты
npm run test:e2e              # E2E тесты
npm run test:all              # Все тесты
npm run test:coverage         # С покрытием

# Docker окружение
npm run docker:test:up        # Запустить
npm run docker:test:down      # Остановить
npm run docker:test:logs      # Логи
npm run docker:test:clean     # Очистить
```

---

## 🐛 Если что-то сломалось

### Docker не запускается

```bash
npm run docker:test:clean
npm run docker:test:up
sleep 30
```

### Тесты падают

```bash
# Проверь статус контейнеров
docker-compose -f docker-compose.test.yml ps

# Проверь логи
npm run docker:test:logs
```

### PostgreSQL не инициализирован

```bash
docker volume rm docker-mcp-server_postgres_test_data
npm run docker:test:up
sleep 30
```

---

## 📚 Детальная документация

- **[TEST_SYSTEM.md](./TEST_SYSTEM.md)** — Полное описание системы тестирования
- **[MANUAL_TEST.md](./MANUAL_TEST.md)** — Детальный чек-лист для ручного тестирования
- **[pre-commit.sh](./pre-commit.sh)** — Скрипт пре-коммита
- **[tests/e2e/](./tests/e2e/)** — E2E тесты

---

## ✅ Чек-лист перед коммитом

- [ ] `./pre-commit.sh` успешно завершился
- [ ] Все тесты прошли (зеленые)
- [ ] Ручное smoke тестирование выполнено
- [ ] Документация обновлена (если нужно)
- [ ] CHANGELOG.md обновлен (если нужно)

---

## 🎓 Best Practice

1. **Перед началом работы:** `npm run docker:test:up`
2. **Во время разработки:** `npm run test:watch`
3. **Перед коммитом:** `./pre-commit.sh`
4. **После коммита:** Проверь CI/CD (если настроен)

---

**Вопросы?** Смотри [TEST_SYSTEM.md](./TEST_SYSTEM.md) для деталей.
