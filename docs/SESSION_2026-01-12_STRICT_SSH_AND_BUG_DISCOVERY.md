# Сессия 2026-01-12: Строгая проверка SSH + Обнаружение бага

> **Цель сессии:** Добавить строгую проверку SSH ключей  
> **Результат:** Добавили проверку + обнаружили критичный баг с кэшированием

---

## 📋 Что было сделано

### 1. Добавлена строгая проверка SSH ключей (v1.3.2)

**Проблема:** SSH ключ мог не существовать, но подключение работало через fallback механизм SSH.

**Решение:**

```typescript
// src/utils/docker-client.ts
if (config.privateKeyPath) {
  const keyPath = this.resolveKeyPath(config.privateKeyPath);
  
  if (!existsSync(keyPath)) {
    const errorMsg = `SSH private key not found: ${keyPath}
    
    Possible solutions:
      1. Check the path in your profile
      2. Create the key: ssh-keygen -t ed25519...
      3. Use SSH Agent (remove privateKeyPath)
      4. Enable fallback: DOCKER_MCP_ALLOW_SSH_FALLBACK=true`;
    
    if (process.env.DOCKER_MCP_ALLOW_SSH_FALLBACK === 'true') {
      logger.warn(errorMsg + "\n\nFallback enabled...");
    } else {
      throw new Error(errorMsg);  // ← СТРОГАЯ ПРОВЕРКА!
    }
  }
}
```

**Преимущества:**
- ✅ Явная ошибка вместо silent fallback
- ✅ Понятные сообщения с решениями
- ✅ Опциональный legacy режим через ENV
- ✅ Безопасность — нет случайного использования дефолтного ключа

**Файлы:**
- `src/utils/docker-client.ts` — строгая проверка
- `src/utils/ssh-config.ts` — улучшенные warning
- `package.json` — версия 1.3.2
- `CHANGELOG.md` — полное описание + migration guide

---

## 🐛 Обнаружен критичный баг (BUG-011)

### Проблема: Docker Client кэшируется по host

При тестировании строгой проверки обнаружили:

```
Профиль с НЕСУЩЕСТВУЮЩИМ ключом → Подключение РАБОТАЕТ! ❌
```

### Расследование

```
docker_container_list({ profile: "test-bad-key" })
    ↓
privateKeyPath: "~/.ssh/id_ed25519_NONEXISTENT"  ← Не существует!
    ↓
Ожидаем: ОШИБКА ❌
Получили: "No containers found" ✅  ← РАБОТАЕТ?!
```

### Корень проблемы

```typescript
// docker-client.ts:471-475
const shouldRecreate = !dockerClientInstance || 
  (sshConfig !== undefined && (
    dockerClientInstance.isRemote !== requestedIsRemote ||
    dockerClientInstance.getSSHHost() !== requestedHost  // ← ТОЛЬКО HOST!
  ));
```

**Баг:** Кэш использует только `host`, игнорирует `privateKeyPath`!

### Сценарий бага

```
1. Profile "prod-admin":
   host: prod.example.com
   key: ~/.ssh/id_rsa_admin ✅
   → Создается DockerClient
   → Кэш: key="prod.example.com", value=этот клиент

2. Profile "prod-readonly":
   host: prod.example.com  ← ТОТ ЖЕ HOST!
   key: ~/.ssh/id_rsa_readonly
   → getDockerClient() проверяет кэш
   → Находит по host="prod.example.com"
   → Возвращает СТАРЫЙ клиент (с admin ключом!)
   → Строгая проверка НЕ срабатывает
```

### Последствия

1. **Security issue** — разные профили могут использовать не тот ключ
2. **Строгая проверка обходится** — баг делает v1.3.2 бесполезной
3. **Непредсказуемое поведение** — зависит от порядка вызовов

---

## 📊 Анализ архитектуры

### Две системы кэширования

#### Старая система (используется, с багом)

```typescript
getDockerClient(sshConfig)
    ↓
Singleton: dockerClientInstance
Ключ кэша: host
Проблема: Не учитывает privateKeyPath
```

#### Новая система (есть, но НЕ используется)

```typescript
getDockerClientForProfile(profileName)
    ↓
Pool: Map<profileName, DockerClient>
Ключ кэша: profileName
Правильно: Каждый профиль = свой клиент
```

### Где используется старая система

```
ContainerTools → resolveSSHConfig() → SSHConfig → ContainerManager(sshConfig) → getDockerClient(sshConfig)
ExecutorTool   → resolveSSHConfig() → SSHConfig → (managers)              → getDockerClient(sshConfig)
DatabaseTools  → resolveSSHConfig() → SSHConfig → (adapters)              → getDockerClient(sshConfig)
EnvTools       → resolveSSHConfig() → SSHConfig → (managers)              → getDockerClient(sshConfig)
```

**Вывод:** Вся система использует старую архитектуру с багом!

---

## ✅ Решение: Рефакторинг на Profile Pool

### План

**Цель:** Мигрировать на `getDockerClientForProfile(profileName)`

**Архитектура:**

```
MCP Tool → args.profile → Manager(profileName) → getDockerClientForProfile(profileName)
                                                        ↓
                                            Pool кэш по PROFILE NAME ✅
```

**Оценка:** 13-17 часов (~2 рабочих дня)

**Версия:** 1.4.0 (breaking change в API менеджеров)

### Документация

Создано:
- ✅ `docs/REFACTORING_PROFILE_CLIENT_POOL.md` — полный план (50+ страниц)
- ✅ `docs/REFACTORING_QUICK_START.md` — быстрый старт для нового агента
- ✅ `docs/BUGS_PROFILE_SSH.md` — обновлен с BUG-011
- ✅ `docs/SESSION_2026-01-12_STRICT_SSH_AND_BUG_DISCOVERY.md` — эта сессия

---

## 📈 Прогресс версий

```
v1.3.1 → v1.3.2 → v1.4.0 (planned)
   ↓         ↓         ↓
  Bug    Strict    Profile
  Fix    Check     Pool
```

### v1.3.2 (завершено сегодня)

✅ Строгая проверка SSH ключей  
✅ CHANGELOG обновлен  
✅ package.json + package-lock.json  
✅ Build успешно

### v1.4.0 (планируется)

📋 Рефакторинг на Profile Pool  
📋 Исправление BUG-011  
📋 Breaking change в API менеджеров  
📋 Migration guide

---

## 🎯 Следующие шаги

### Немедленно (в новом агенте)

1. **Прочитать документацию:**
   - `docs/REFACTORING_PROFILE_CLIENT_POOL.md` — полный план
   - `docs/REFACTORING_QUICK_START.md` — быстрый старт

2. **Создать feature branch:**
   ```bash
   git checkout -b refactor/profile-client-pool
   ```

3. **Запустить baseline тесты:**
   ```bash
   npm run test
   npm run test:e2e
   ```

4. **Следовать плану:**
   - Phase 1: Анализ зависимостей
   - Phase 2: Рефакторинг Managers
   - Phase 3: Рефакторинг Tools
   - Phase 4: Рефакторинг Adapters
   - Phase 5: Cleanup старого кода
   - Phase 6: Тесты
   - Phase 7: Документация
   - Phase 8: Релиз v1.4.0

### Важные проверки

- [ ] Строгая проверка SSH работает после рефакторинга
- [ ] Два профиля с одним host используют разные клиенты
- [ ] Производительность не ухудшилась
- [ ] Все E2E тесты проходят

---

## 📚 Созданные документы

1. **REFACTORING_PROFILE_CLIENT_POOL.md** (7000+ слов)
   - Описание проблемы с диаграммами
   - 8 фаз рефакторинга
   - Примеры кода для каждого файла
   - Оценка времени
   - Риски и митигация
   - Checklist

2. **REFACTORING_QUICK_START.md** (1500+ слов)
   - Краткий саммари
   - Ключевые файлы
   - Примеры изменений
   - Критичный тест
   - Команды для запуска

3. **BUGS_PROFILE_SSH.md** (обновлен)
   - Добавлен BUG-011
   - История бага
   - Воспроизведение
   - Последствия

4. **SESSION_2026-01-12_STRICT_SSH_AND_BUG_DISCOVERY.md** (этот файл)
   - Что было сделано
   - Как обнаружили баг
   - Анализ архитектуры
   - Следующие шаги

---

## 💡 Выводы

### Что узнали

1. **SSH fallback работает неявно:**
   - SSH клиент автоматически пробует дефолтные ключи
   - Это создавало ложное ощущение корректности
   - Строгая проверка выявила настоящую проблему

2. **Кэширование — source of bugs:**
   - Singleton по host был ошибочным дизайном
   - Profile pool — правильная архитектура
   - Нужно было использовать её с самого начала

3. **Тестирование выявляет баги:**
   - Простой тест "плохой ключ → ошибка" выявил критичный баг
   - Важно тестировать edge cases
   - E2E тесты должны покрывать разные профили

### Уроки на будущее

1. **Кэширование требует продуманного ключа:**
   - Не использовать partial data (только host)
   - Использовать уникальные идентификаторы (profile name)
   - Документировать логику кэширования

2. **Строгая валидация — первый приоритет:**
   - Fail-fast лучше, чем silent fallback
   - Explicit is better than implicit
   - Понятные error messages с решениями

3. **Архитектура должна эволюционировать:**
   - Старая система → работает, но с багами
   - Новая система → готова, но не используется
   - Рефакторинг → необходим для прогресса

---

## 📞 Для следующего агента

**Контекст готов!** Все документы созданы, план детальный.

**Начинай с:**
1. Прочитать `REFACTORING_QUICK_START.md`
2. Затем `REFACTORING_PROFILE_CLIENT_POOL.md`
3. Создать branch и начать Phase 1

**Ожидаемое время:** 2 рабочих дня (13-17 часов)

**Версия после завершения:** 1.4.0

**Главная цель:** Исправить BUG-011 и улучшить архитектуру.

---

**Конец сессии:** 2026-01-12  
**Статус:** ✅ Документация готова к рефакторингу  
**Следующий шаг:** Создать новый агент для выполнения рефакторинга
