# Sprint 5: Remote Docker — SSH Support

**Статус:** 🟢 ЗАВЕРШЕНО  
**Длительность:** 1 неделя (5 рабочих дней)  
**Приоритет:** 🟡 Средний  
**Дата начала:** 2026-01-08
**Дата завершения:** 2026-01-09  
**Обновлено:** 2026-01-09 (оптимизация и тестирование)

---

## 🎯 Цель Спринта

Добавить поддержку удаленного Docker через SSH. После завершения спринта можно будет управлять Docker контейнерами на удаленных серверах через SSH соединение.

**Ключевые возможности:**
- ✅ Подключение к удаленному Docker через SSH (ключ или пароль)
- ✅ Переключение между несколькими серверами (профили)
- ✅ Автоматическое переподключение при потере связи (retry 3, timeout 30s)
- ✅ Безопасная конфигурация через env переменные или параметры
- ✅ Обратная совместимость с локальным Docker
- ✅ **НОВОЕ:** Remote Project Discovery — автоматический поиск проектов на удаленном сервере
- ✅ **НОВОЕ:** SSH File Access — чтение docker-compose.yml через SSH
- ✅ **НОВОЕ:** Project parameter — явное указание проекта для всех команд

---

## ✅ Выполненные Задачи (2026-01-09)

### Remote Project Discovery (NEW)

#### Задача 5.13: SSH Exec Module
- [x] Создать `src/utils/ssh-exec.ts`
- [x] Реализовать `execSSH()` — выполнение команд через SSH
- [x] Реализовать `readRemoteFile()` — чтение файлов через SSH
- [x] Реализовать `findRemoteFiles()` — поиск файлов через SSH
- [x] Добавить поддержку timeout и error handling

**Файлы:**
- `src/utils/ssh-exec.ts` ✅

#### Задача 5.14: Remote Project Discovery
- [x] Создать `src/discovery/remote-discovery.ts`
- [x] Реализовать `RemoteProjectDiscovery` класс
- [x] Реализовать `discoverProjects()` — поиск всех проектов
- [x] Реализовать `getProjectStatus()` — статус конкретного проекта
- [x] Интеграция с Docker API для matching containers ↔ services
- [x] Определение статусов: running, partial, stopped
- [x] Обнаружение issues (restarting, exited containers)

**Файлы:**
- `src/discovery/remote-discovery.ts` ✅
- `src/discovery/compose-parser.ts` (добавлен `parseFromString()`) ✅

#### Задача 5.15: Discovery Tools
- [x] Создать `src/tools/discovery-tools.ts`
- [x] Реализовать `docker_discover_projects` команду
- [x] Реализовать `docker_project_status` команду
- [x] Интеграция в `src/index.ts`

**Файлы:**
- `src/tools/discovery-tools.ts` ✅
- `src/index.ts` (обновлен) ✅

#### Задача 5.16: Project Parameter Support
- [x] Добавить `project` параметр во все команды:
  - `docker_container_list`
  - `docker_container_start/stop/restart`
  - `docker_container_logs`
  - `docker_container_stats`
  - `docker_exec`
  - `docker_db_query/backup/restore/status`
- [x] Обновить `ProjectDiscovery.findProject()` для поддержки `explicitProjectName`
- [x] Обновить все tools для использования project параметра

**Файлы:**
- `src/discovery/project-discovery.ts` ✅
- `src/discovery/types.ts` ✅
- `src/tools/container-tools.ts` ✅
- `src/tools/executor-tool.ts` ✅
- `src/tools/database-tools.ts` ✅

#### Задача 5.17: Profiles Configuration Enhancement
- [x] Добавить `projectsPath` в `SSHProfileData` интерфейс
- [x] Обновить `loadProfilesFile()` для поддержки `projectsPath`
- [x] Использовать `projectsPath` из профиля как default для discovery

**Файлы:**
- `src/utils/profiles-file.ts` ✅

#### Задача 5.18: Tests for Remote Functions
- [x] Создать `tests/unit/utils/docker-client.test.ts` (21 тест)
- [x] Создать `tests/unit/tools/profile-tool.test.ts` (13 тестов)
- [x] Создать `tests/integration/remote-docker.test.ts` (12 тестов)
- [x] Все тесты проходят успешно (46 тестов)

**Файлы:**
- `tests/unit/utils/docker-client.test.ts` ✅
- `tests/unit/tools/profile-tool.test.ts` ✅
- `tests/integration/remote-docker.test.ts` ✅

---

## 📋 Задачи

### День 1: SSH Configuration Module

#### Задача 5.1: SSH Config Types & Interfaces
- [ ] Создать `src/utils/ssh-config.ts`
- [ ] Определить интерфейс `SSHConfig` с полями:
  - `host: string` — адрес сервера
  - `port?: number` — порт SSH (по умолчанию 22)
  - `username: string` — имя пользователя
  - `privateKeyPath?: string` — путь к SSH ключу
  - `passphrase?: string` — пароль для зашифрованного ключа
  - `password?: string` — пароль для аутентификации (не рекомендуется)
- [ ] Определить интерфейс `SSHProfile` для множественных серверов:
  - `name: string` — имя профиля
  - `config: SSHConfig` — конфигурация SSH
- [ ] Создать функцию `loadSSHConfig()` для загрузки из env
- [ ] Создать функцию `loadSSHProfiles()` для загрузки профилей
- [ ] Добавить валидацию конфигурации (host, username обязательны)

**Файлы:**
- `src/utils/ssh-config.ts`

**Критерии готовности:**
- ✅ Типы определены
- ✅ Функции загрузки работают
- ✅ Валидация корректна
- ✅ Unit тесты написаны

**Пример интерфейса:**
```typescript
export interface SSHConfig {
  host: string;
  port?: number;
  username: string;
  privateKeyPath?: string;
  passphrase?: string;
  password?: string;
}

export interface SSHProfile {
  name: string;
  config: SSHConfig;
}
```

---

#### Задача 5.2: SSH Config Loading from Environment
- [ ] Реализовать загрузку из `DOCKER_SSH_HOST`, `DOCKER_SSH_USER`, etc.
- [ ] Реализовать загрузку из `DOCKER_SSH_PROFILE` (имя профиля)
- [ ] Поддержать множественные профили через `DOCKER_SSH_PROFILES` (JSON)
- [ ] Добавить поддержку `~/.ssh/config` (опционально)
- [ ] Реализовать безопасную обработку путей к ключам (path validation)

**Файлы:**
- `src/utils/ssh-config.ts`

**Переменные окружения:**
```bash
# Простой вариант (один сервер)
DOCKER_SSH_HOST=example.com
DOCKER_SSH_PORT=22
DOCKER_SSH_USER=deployer
DOCKER_SSH_KEY=/path/to/id_rsa
DOCKER_SSH_PASSPHRASE=secret  # опционально

# Профили (несколько серверов)
DOCKER_SSH_PROFILE=production
DOCKER_SSH_PROFILES={"production":{"host":"prod.com","username":"deployer"},"staging":{"host":"staging.com","username":"deployer"}}
```

**Критерии готовности:**
- ✅ Загрузка из env работает
- ✅ Профили поддерживаются
- ✅ Path validation предотвращает directory traversal
- ✅ Обработка ошибок корректна

---

### День 2: Docker Client SSH Integration

#### Задача 5.3: DockerClient SSH Support
- [ ] Модифицировать `src/utils/docker-client.ts`
- [ ] Добавить параметр `sshConfig?: SSHConfig` в конструктор `DockerClient`
- [ ] Реализовать создание Dockerode с SSH:
  ```typescript
  if (sshConfig) {
    this.docker = new Docker({
      protocol: 'ssh',
      host: sshConfig.host,
      port: sshConfig.port || 22,
      username: sshConfig.username,
      sshOptions: {
        privateKey: fs.readFileSync(sshConfig.privateKeyPath),
        passphrase: sshConfig.passphrase,
      },
    });
  }
  ```
- [ ] Добавить поддержку пароля (если `password` указан)
- [ ] Обновить функцию `getDockerClient()` для передачи SSH конфига
- [ ] Добавить проверку SSH соединения в `ping()`

**Файлы:**
- `src/utils/docker-client.ts`

**Критерии готовности:**
- ✅ Dockerode создается с SSH параметрами
- ✅ Подключение к удаленному Docker работает
- ✅ `ping()` проверяет SSH соединение
- ✅ Обработка ошибок SSH корректна

---

#### Задача 5.4: Retry & Timeout Logic
- [ ] Создать функцию `retryWithTimeout()` в `src/utils/retry.ts`
- [ ] Реализовать retry логику (3 попытки)
- [ ] Реализовать timeout (30 секунд на операцию)
- [ ] Добавить экспоненциальный backoff между попытками
- [ ] Интегрировать retry в `DockerClient.ping()`
- [ ] Интегрировать retry в критические операции (listContainers, exec)

**Файлы:**
- `src/utils/retry.ts` (новый)
- `src/utils/docker-client.ts`

**Критерии готовности:**
- ✅ Retry работает (3 попытки)
- ✅ Timeout работает (30s)
- ✅ Backoff реализован
- ✅ Логирование попыток подключения

---

### День 3: Compose Exec SSH Support

#### Задача 5.5: Docker Context Support
- [ ] Модифицировать `src/utils/compose-exec.ts`
- [ ] Добавить параметр `dockerContext?: string` в метод `run()`
- [ ] Реализовать использование Docker context:
  ```typescript
  const contextArg = dockerContext ? `--context ${dockerContext}` : '';
  const cmd = `docker-compose ${contextArg} -f ${composeFile} ${args.join(' ')}`;
  ```
- [ ] Добавить функцию `createDockerContext()` для автоматического создания контекста
- [ ] Обновить `ComposeManager` для передачи контекста

**Файлы:**
- `src/utils/compose-exec.ts`
- `src/managers/compose-manager.ts`

**Альтернативный вариант (DOCKER_HOST):**
Если Docker context не работает, использовать `DOCKER_HOST` env переменную:
```typescript
const env = sshConfig 
  ? { ...process.env, DOCKER_HOST: `ssh://${sshConfig.username}@${sshConfig.host}` }
  : process.env;
```

**Критерии готовности:**
- ✅ Docker context поддерживается
- ✅ docker-compose работает с удаленным Docker
- ✅ Fallback на DOCKER_HOST работает (если нужно)

---

#### Задача 5.6: SSH Profile Management
- [ ] Создать `src/utils/ssh-profile-manager.ts`
- [ ] Реализовать переключение между профилями
- [ ] Реализовать функцию `getActiveProfile()` — текущий активный профиль
- [ ] Реализовать функцию `switchProfile(name: string)` — переключение профиля
- [ ] Добавить валидацию профилей при переключении
- [ ] Интегрировать в `DockerClient` и `ComposeExec`

**Файлы:**
- `src/utils/ssh-profile-manager.ts` (новый)
- `src/utils/docker-client.ts`
- `src/utils/compose-exec.ts`

**Критерии готовности:**
- ✅ Переключение профилей работает
- ✅ Валидация профилей корректна
- ✅ Активный профиль сохраняется между вызовами

---

### День 4: Integration & Testing

#### Задача 5.7: Main Server Integration
- [ ] Модифицировать `src/index.ts`
- [ ] Загрузить SSH конфиг при старте сервера
- [ ] Передать SSH конфиг в `getDockerClient()`
- [ ] Обновить логирование (указать локальный/удаленный Docker)
- [ ] Добавить проверку SSH соединения при инициализации

**Файлы:**
- `src/index.ts`

**Критерии готовности:**
- ✅ SSH конфиг загружается при старте
- ✅ DockerClient создается с SSH параметрами
- ✅ Логирование указывает тип подключения
- ✅ Ошибки SSH обрабатываются корректно

---

#### Задача 5.8: Managers Integration
- [ ] Обновить `ContainerManager` для работы с SSH (если нужно)
- [ ] Обновить `ComposeManager` для передачи Docker context
- [ ] Обновить `DatabaseTools` для работы с удаленными БД (если нужно)
- [ ] Обновить `ExecutorTool` для работы с удаленными контейнерами
- [ ] Проверить обратную совместимость (локальный Docker должен работать)

**Файлы:**
- `src/managers/container-manager.ts`
- `src/managers/compose-manager.ts`
- `src/tools/database-tools.ts`
- `src/tools/executor-tool.ts`

**Критерии готовности:**
- ✅ Все менеджеры работают с SSH
- ✅ Обратная совместимость сохранена
- ✅ Локальный Docker работает без изменений

---

#### Задача 5.9: Unit Tests
- [ ] Создать `tests/unit/ssh-config.test.ts`
- [ ] Тесты для загрузки SSH конфига из env
- [ ] Тесты для валидации конфигурации
- [ ] Тесты для профилей
- [ ] Создать `tests/unit/retry.test.ts`
- [ ] Тесты для retry логики
- [ ] Тесты для timeout
- [ ] Моки для SSH соединения

**Файлы:**
- `tests/unit/ssh-config.test.ts` (новый)
- `tests/unit/retry.test.ts` (новый)

**Критерии готовности:**
- ✅ Unit тесты написаны
- ✅ Покрытие > 80%
- ✅ Все тесты проходят

---

### День 5: Documentation & GraphML

#### Задача 5.10: Documentation
- [ ] Обновить `README.md` — добавить секцию про Remote Docker
- [ ] Создать `docs/REMOTE_DOCKER.md` — полное руководство:
  - Установка и настройка
  - Конфигурация SSH
  - Примеры использования
  - Troubleshooting
  - Security best practices
- [ ] Обновить `docs/SETUP_INSTRUCTIONS.md` — добавить SSH настройку
- [ ] Обновить `docs/EXAMPLES.md` — добавить примеры удаленного Docker

**Файлы:**
- `README.md`
- `docs/REMOTE_DOCKER.md` (новый)
- `docs/SETUP_INSTRUCTIONS.md`
- `docs/EXAMPLES.md`

**Критерии готовности:**
- ✅ Документация полная и понятная
- ✅ Примеры работают
- ✅ Troubleshooting покрывает частые проблемы

---

#### Задача 5.11: GraphML Architecture Update
- [ ] Открыть `docs/graphml/architecture.graphml`
- [ ] Добавить узел `ssh-config` (SSH Configuration Module)
- [ ] Добавить узел `ssh-profile-manager` (SSH Profile Manager)
- [ ] Добавить узел `retry` (Retry & Timeout Utility)
- [ ] Добавить узел `remote-docker-engine` (Remote Docker Engine)
- [ ] Добавить связи:
  - `docker-client` → `ssh-config`
  - `docker-client` → `retry`
  - `ssh-config` → `ssh-profile-manager`
  - `docker-client` → `remote-docker-engine` (через SSH)
- [ ] Обновить описание узлов (добавить ✅ для новых модулей)
- [ ] Обновить `docs/graphml/README.md` с описанием изменений

**Файлы:**
- `docs/graphml/architecture.graphml`
- `docs/graphml/README.md`

**Критерии готовности:**
- ✅ GraphML обновлен
- ✅ Все новые модули добавлены
- ✅ Связи корректны
- ✅ README обновлен

---

#### Задача 5.12: Integration Testing
- [ ] Протестировать подключение к удаленному Docker
- [ ] Протестировать `docker_container_list` на удаленном сервере
- [ ] Протестировать `docker_compose_up` на удаленном сервере
- [ ] Протестировать `docker_exec` на удаленном контейнере
- [ ] Протестировать переключение между профилями
- [ ] Протестировать retry при потере связи
- [ ] Протестировать обратную совместимость (локальный Docker)

**Критерии готовности:**
- ✅ Все команды работают с удаленным Docker
- ✅ Переключение профилей работает
- ✅ Retry работает корректно
- ✅ Локальный Docker работает без изменений

---

## 📊 Метрики Успеха

- ✅ Подключение к удаленному Docker через SSH работает
- ✅ Все существующие команды работают с удаленным Docker
- ✅ Переключение между несколькими серверами работает
- ✅ Retry и timeout работают корректно
- ✅ Обратная совместимость сохранена
- ✅ Документация полная и понятная
- ✅ GraphML архитектура обновлена

---

## 🔗 Связанные Документы

- [Architecture](../ARCHITECTURE.md)
- [Developer Architecture](../DEV/DEVELOPER_ARCHITECTURE.md)
- [GraphML Architecture](../graphml/architecture.graphml)
- [API Reference](../API_REFERENCE.md)
- [All Sprints](./SPRINTS.md)

---

## 📝 Технические Детали

### Архитектура SSH подключения

```
┌─────────────────────────────────────────────────────────┐
│                    MCP Server (index.ts)                │
│                   (локальная машина)                    │
└─────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────┐
│           SSH Config Module (ssh-config.ts)             │
│         Загрузка конфигурации из env/профилей           │
└─────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────┐
│           DockerClient (docker-client.ts)               │
│                                                          │
│   if (SSH_CONFIG):                                      │
│     new Docker({ ssh: "ssh://user@host" })             │
│     + retry logic (3 attempts, 30s timeout)             │
│   else:                                                 │
│     new Docker() // локально                            │
└─────────────────────────────────────────────────────────┘
                            │
                            ▼
                    ╔═══════════════╗
                    ║  SSH Tunnel   ║
                    ╚═══════════════╝
                            │
                            ▼
┌─────────────────────────────────────────────────────────┐
│              Удаленный Docker Server                    │
│            /var/run/docker.sock (remote)               │
└─────────────────────────────────────────────────────────┘
```

### Методы аутентификации

1. **SSH ключ (рекомендуется)** ✅
   - `privateKeyPath` — путь к приватному ключу
   - `passphrase` — пароль для зашифрованного ключа (опционально)

2. **Пароль (не рекомендуется)** ⚠️
   - `password` — пароль для SSH аутентификации
   - Только для тестирования, не использовать в production

### Конфигурация

**Вариант 1: Environment Variables (простой)**
```bash
DOCKER_SSH_HOST=example.com
DOCKER_SSH_USER=deployer
DOCKER_SSH_KEY=/path/to/id_rsa
```

**Вариант 2: Profiles (множественные серверы)**
```bash
DOCKER_SSH_PROFILE=production
DOCKER_SSH_PROFILES={"production":{"host":"prod.com","username":"deployer"},"staging":{"host":"staging.com","username":"deployer"}}
```

**Вариант 3: MCP Config (Cursor/Claude Desktop)**
```json
{
  "mcpServers": {
    "docker": {
      "command": "docker-mcp-server",
      "env": {
        "DOCKER_SSH_HOST": "example.com",
        "DOCKER_SSH_USER": "deployer",
        "DOCKER_SSH_KEY": "/Users/me/.ssh/id_rsa"
      }
    }
  }
}
```

### Retry & Timeout

- **Retry:** 3 попытки
- **Timeout:** 30 секунд на операцию
- **Backoff:** Экспоненциальный (1s, 2s, 4s)
- **Применяется к:** ping, listContainers, exec, compose commands

### Безопасность

- ✅ SSH ключи безопаснее паролей
- ✅ Пароли НЕ хранятся в коде/конфигах
- ✅ Path validation предотвращает directory traversal
- ✅ Валидация конфигурации перед использованием
- ✅ Логирование без чувствительных данных

---

## ⚠️ Важные Замечания

1. **Тестирование:** Требуется реальный SSH сервер для полного тестирования
2. **Производительность:** SSH добавляет латентность (~50-200ms на команду)
3. **Совместимость:** Требует установленный SSH клиент на машине
4. **Docker Context:** Может потребоваться создание Docker context вручную
5. **Обратная совместимость:** Локальный Docker должен работать без изменений

---

## 📝 Примечания

- **Технологии:** Dockerode SSH support, Node.js SSH client
- **Тестирование:** Unit тесты + Integration тесты на реальном SSH сервере
- **Документация:** Полное руководство по настройке и использованию
- **GraphML:** Обновление архитектурной диаграммы обязательно

---

**Обновлено:** 2026-01-09  
**Версия:** 1.1.0  
**Статус:** 🟢 ЗАВЕРШЕНО

---

## 📝 Итоги Спринта

### Реализовано

1. **Remote Docker Support** ✅
   - SSH tunnel создание и управление
   - Retry логика с exponential backoff
   - Healthcheck для SSH туннеля
   - Cleanup при shutdown

2. **Remote Project Discovery** ✅
   - Автоматический поиск проектов на remote сервере
   - Чтение docker-compose.yml через SSH
   - Matching containers с services
   - Определение статусов проектов

3. **Project Parameter** ✅
   - Поддержка явного указания проекта во всех командах
   - Backward compatible (auto-detect для local)
   - Работает для remote Docker

4. **Profiles Enhancement** ✅
   - Поддержка `projectsPath` в profiles.json
   - Автоматическое использование из конфига

5. **Testing** ✅
   - 46 тестов для remote функций
   - Unit тесты для DockerClient
   - Integration тесты для remote scenarios
   - Все тесты проходят

### Новые Команды

- `docker_discover_projects` — найти все проекты на remote сервере
- `docker_project_status` — детальный статус проекта

### Обновленные Команды

Все команды теперь поддерживают опциональный `project` параметр:
- `docker_container_list({ project: "gobunnygo" })`
- `docker_container_logs("service", { project: "gobunnygo" })`
- `docker_exec("service", "cmd", { project: "gobunnygo" })`
- И т.д.

### Оптимизация производительности (2026-01-09)

#### Задача 5.19: Оптимизация Remote Discovery
- [x] Убрать параметр `mode` из `discoverProjects()` - всегда fast mode
- [x] Удалить метод `discoverProjectsFull()` - не нужен
- [x] Оптимизировать `getRunningContainersViaSSH()` - batch inspect одной командой
- [x] Исправить зависание процесса - `unref()` для healthcheck interval
- [x] Обновить документацию с REST API подходом

**Изменения:**
- `discoverProjects()` теперь всегда использует fast mode (только Docker labels, ~2s)
- `getProjectStatus()` всегда использует full mode (читает compose для конкретного проекта, ~3s)
- Убрана необходимость парсить все compose файлы для списка проектов
- Исправлено зависание Node.js процесса после SSH tunnel создания

**Файлы:**
- `src/discovery/remote-discovery.ts` ✅
- `src/tools/discovery-tools.ts` ✅
- `src/utils/docker-client.ts` ✅
- `docs/REMOTE_DISCOVERY.md` ✅

#### Задача 5.20: Сравнительное тестирование
- [x] Создать `test-mcp-vs-ssh-comparison.js` - сравнительный тест
- [x] Создать `TEST_COMPARISON.md` - детальный отчет
- [x] Протестировать полноту ответа, удобство, скорость
- [x] Вывести вердикт: MCP превосходит SSH по всем критериям

**Результаты тестирования:**
- Полнота ответа: MCP 10/10, SSH 4/10
- Удобство использования: MCP 10/10, SSH 3/10
- Скорость ответа: MCP 9/10, SSH 6/10
- **Итого: MCP 29/30, SSH 13/30**

**Файлы:**
- `test-mcp-vs-ssh-comparison.js` ✅
- `TEST_COMPARISON.md` ✅

### Статистика

- **Новых файлов:** 6
- **Обновленных файлов:** 10
- **Новых команд:** 2
- **Обновленных команд:** 15+
- **Тестов:** 46 (все проходят)
- **Строк кода:** ~2000+
- **Сравнительных тестов:** 1 (MCP vs SSH)

