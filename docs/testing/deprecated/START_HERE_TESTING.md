# 🚀 START HERE - Testing System

## ⚡ Один шаг перед коммитом

```bash
./pre-commit.sh
```

**Готово!** Эта команда делает ВСЁ за тебя.

---

## 🎯 Что создано

Полная система тестирования для Docker MCP Server:

✅ **Тестовое окружение** (docker-compose.test.yml)
✅ **E2E тесты** для всех 20 MCP команд
✅ **Pre-commit скрипт** (один шаг!)
✅ **5 документов** с инструкциями
✅ **NPM команды** для всех операций

---

## 📁 Ключевые файлы

| Файл | Что делает |
|------|-----------|
| **./pre-commit.sh** | ⭐ ГЛАВНАЯ КОМАНДА - запускай перед коммитом |
| **MANUAL_TEST.md** | Чек-лист для ручного тестирования (AI) |
| **docker-compose.test.yml** | Тестовое окружение (web, postgres, redis) |
| **tests/e2e/mcp-tools.test.ts** | E2E тесты всех 20 команд |

---

## 🚀 Quick Start (3 минуты)

### 1. Установка

```bash
npm install
npm run build
```

### 2. Запуск окружения

```bash
npm run docker:test:up
sleep 30
```

### 3. Тестирование

```bash
./pre-commit.sh
```

---

## ✅ Что покрыто

**20 MCP команд полностью протестировано:**

- ✅ Container Tools (9): list, start, stop, restart, logs, stats, resources
- ✅ Database Tools (4): status, query, backup (PostgreSQL, Redis)
- ✅ Executor Tool (1): exec
- ✅ Env Tools (3): env_list, compose_config, healthcheck
- ✅ Utility Tools (2): mcp_health, profile_info
- ✅ Discovery Tools (1): projects

**Дополнительно:**
- ✅ Profile parameter (LOCAL/REMOTE)
- ✅ Error handling
- ✅ Edge cases

---

## 📚 Документация

Читай в зависимости от цели:

| Цель | Документ |
|------|----------|
| **Быстрый старт** | [TESTING_QUICK_START.md](./TESTING_QUICK_START.md) |
| **Ежедневная работа** | [README_TESTING.md](./README_TESTING.md) |
| **AI тестирование** | [MANUAL_TEST.md](./MANUAL_TEST.md) ⭐ |
| **Детали системы** | [TEST_SYSTEM.md](./TEST_SYSTEM.md) |
| **Overview** | [TESTING_SUMMARY.md](./TESTING_SUMMARY.md) |

---

## 🎓 Workflow

### Перед началом работы (один раз)

```bash
npm install
npm run build
npm run docker:test:up
sleep 30
```

### Во время разработки

```bash
npm run test:watch    # Unit тесты в watch режиме
```

### Перед коммитом (ВСЕГДА!)

```bash
./pre-commit.sh
```

### Коммит

```bash
git add .
git commit -m "your message"
git push
```

---

## 🐛 Troubleshooting

### Docker не запускается

```bash
npm run docker:test:clean
npm run docker:test:up
sleep 30
```

### Тесты падают

```bash
docker-compose -f docker-compose.test.yml ps
npm run docker:test:logs
```

### PostgreSQL не инициализирован

```bash
docker volume rm docker-mcp-server_postgres_test_data
npm run docker:test:up
sleep 30
```

---

## 📊 Метрики

- **Покрытие:** 20/20 команд (100%)
- **E2E тесты:** 40+ тест-кейсов
- **Время:** ~5-10 минут
- **Результат:** Уверенность в качестве

---

## 💡 Best Practice

**DO ✅:**
- ВСЕГДА запускай `./pre-commit.sh` перед коммитом
- Используй `npm run test:watch` во время разработки
- Читай ошибки внимательно

**DON'T ❌:**
- НЕ коммить без тестов
- НЕ игнорировать падающие тесты
- НЕ пушить если CI/CD падает

---

## 🎉 Готово!

Система работает **ПРЯМО СЕЙЧАС**.

Просто запусти:

```bash
./pre-commit.sh
```

И всё! 🚀

---

**Нужна помощь?**
- Открой [MANUAL_TEST.md](./MANUAL_TEST.md) для AI тестирования
- Открой [TEST_SYSTEM.md](./TEST_SYSTEM.md) для деталей
- Проверь [Troubleshooting](#troubleshooting)

---

**Дата:** 2026-01-09  
**Версия:** 1.0.0  
**Статус:** ✅ ГОТОВО К ИСПОЛЬЗОВАНИЮ
