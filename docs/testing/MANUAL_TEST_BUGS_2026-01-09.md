# 🐛 Баги найденные при ручном тестировании MCP Tools

**Дата тестирования:** 2026-01-09  
**Версия MCP Server:** 1.2.1 (из package.json)  
**Тестировщик:** AI Assistant (Auto)  
**Тип тестирования:** Ручное тестирование всех 20 MCP инструментов

---

## 📋 Правила документирования

- ✅ **Ошибки MCP сервера** → записываем в этот документ
- ❌ **Ошибки использования/синтаксиса** → исправляем на месте, НЕ записываем

---

## 🐛 НАЙДЕННЫЕ БАГИ

### BUG-001: docker_env_list() без параметра service возвращает пустой результат
**Приоритет:** 🟡 СРЕДНИЙ  
**Статус:** 🔍 НАЙДЕНО

**Описание:**
При вызове `docker_env_list()` без указания параметра `service` возвращается пустой результат "Environment Variables:" без списка переменных.

**Шаги воспроизведения:**
1. Вызвать `docker_env_list()` без параметров
2. Получить результат: "Environment Variables:" (пусто)

**Ожидаемое поведение:**
Должен вернуться список всех переменных окружения из всех сервисов проекта или хотя бы из docker-compose.yml

**Фактическое поведение:**
Возвращается только заголовок "Environment Variables:" без данных

**Окружение:**
- Docker версия: 28.5.1
- MCP Server версия: 1.2.1
- Профиль: LOCAL

**Логи/Ошибки:**
```
Результат: Environment Variables:
(пусто)
```

**Файлы:**
- `src/tools/env-tools.ts` - возможно проблема в логике получения env без service

---

### BUG-002: Redis адаптер не принимает значения с пробелами в кавычках
**Приоритет:** 🟡 СРЕДНИЙ  
**Статус:** 🔍 НАЙДЕНО

**Описание:**
При попытке установить значение Redis ключа с пробелами в кавычках (например, `SET user:1:name "John Doe"` или `SET user:1:name 'John Doe'`) возвращается ошибка "ERR syntax error". Работает только без пробелов или без кавычек.

**Причина:**
В `src/adapters/redis.ts` строка 53 используется `query.trim().split(/\s+/)` для разбиения команды на части. Это разбивает строку по пробелам, игнорируя кавычки, что приводит к неправильному парсингу:
- `SET user:1:name "John Doe"` → `["SET", "user:1:name", "\"John", "Doe\""]` ❌
- `SET user:1:name 'John Doe'` → `["SET", "user:1:name", "'John", "Doe'"]` ❌

**Шаги воспроизведения:**
1. Вызвать `docker_db_query({service: "redis", query: 'SET user:1:name "John Doe"'})`
2. Получить ошибку: "ERR syntax error"
3. Вызвать `docker_db_query({service: "redis", query: 'SET user:1:name \'John Doe\''})`
4. Получить ошибку: "ERR syntax error"
5. Вызвать `docker_db_query({service: "redis", query: 'SET user:1:name JohnDoe'})`
6. Получить успешный результат: "OK"

**Ожидаемое поведение:**
Redis должен принимать значения с пробелами в кавычках, как это работает в стандартном redis-cli:
- `redis-cli SET user:1:name "John Doe"` ✅ работает
- `redis-cli SET user:1:name 'John Doe'` ✅ работает

**Фактическое поведение:**
Возвращается синтаксическая ошибка при использовании кавычек (одинарных или двойных) с пробелами

**Окружение:**
- Docker версия: 28.5.1
- MCP Server версия: 1.2.1
- Профиль: LOCAL
- Redis версия: 7.4.6

**Логи/Ошибки:**
```
Параметры:
- service: redis
- query: SET user:1:name "John Doe"

Результат: ERR syntax error
```

**Файлы:**
- `src/adapters/redis.ts:53-54` - проблема в методе `query()`, используется `split(/\s+/)` который не учитывает кавычки

**Решение:**
Нужно использовать правильный парсинг команд Redis, который учитывает кавычки. Можно использовать библиотеку для парсинга или написать собственный парсер, который правильно обрабатывает кавычки.

---

### BUG-003: docker_compose_config не читает удаленный файл при указании профиля
**Приоритет:** 🟡 СРЕДНИЙ  
**Статус:** ✅ ИСПРАВЛЕНО

**Описание:**
При вызове `docker_compose_config({profile: "remote-profile"})` возвращается конфигурация из локального `docker-compose.yml`, а не из удаленного сервера. Команда игнорирует параметр `profile` и всегда читает локальный файл.

**Причина:**
В `src/tools/env-tools.ts` метод `handleComposeConfig()` использует `getProject(args?.project)`, который не учитывает профиль. `ProjectDiscovery.findProject()` ищет только локальные файлы через `existsSync()`, не зная о remote сервере. Для remote проектов нужно читать compose файл через SSH.

**Шаги воспроизведения:**
1. Создать docker-compose.yml на удаленном сервере с портом `3001:3000`
2. Вызвать `docker_compose_config({profile: "remote-profile"})`
3. Получить результат с портом `3000:3000` (локальный файл) вместо `3001:3000` (удаленный файл)

**Ожидаемое поведение:**
При указании удаленного профиля команда должна читать `docker-compose.yml` с удаленного сервера через SSH и возвращать его конфигурацию.

**Фактическое поведение:**
Команда всегда читает локальный `docker-compose.yml`, игнорируя параметр `profile`.

**Окружение:**
- Docker версия: 28.5.1 (локальный), 29.0.1 (удаленный)
- MCP Server версия: 1.2.1
- Профиль: remote-profile

**Логи/Ошибки:**
```
Локальный файл: ports: - "3000:3000"
Удаленный файл: ports: - "3001:3000"

Результат docker_compose_config({profile: "remote-profile"}):
ports: - 3000:3000  ❌ (должно быть 3001:3000)
```

**Файлы:**
- `src/tools/env-tools.ts` - отсутствовал импорт `loadProfilesFile` и `profileDataToSSHConfig`

**Решение:**
Добавлен статический импорт в начало файла:
```typescript
import { loadProfilesFile, profileDataToSSHConfig } from '../utils/profiles-file.js';
```

**Причина:**
В методе `getSSHConfigForProfile` использовался динамический `require()`, который не работает в ESM контексте. Все остальные тулзы (`container-tools.ts`, `executor-tool.ts`, `discovery-tools.ts`) используют статический импорт в начале файла. После добавления импорта функция стала работать корректно и читает удалённый `docker-compose.yml` через SSH.

---

## ✅ ПРОТЕСТИРОВАННЫЕ ИНСТРУМЕНТЫ

### CONTAINER TOOLS (9)
- [x] docker_container_list ✅
- [x] docker_container_start ✅
- [x] docker_container_stop ✅
- [x] docker_container_restart ✅
- [x] docker_container_logs ✅
- [x] docker_container_stats ✅
- [ ] docker_compose_up (пропущено - не тестируем чтобы не сломать окружение)
- [ ] docker_compose_down (пропущено - не тестируем чтобы не сломать окружение)
- [x] docker_resource_list ✅

### DATABASE TOOLS (4)
- [x] docker_db_query ✅ (есть проблема с Redis пробелами - BUG-002)
- [x] docker_db_backup ✅
- [x] docker_db_restore ✅
- [x] docker_db_status ✅

### EXECUTOR (1)
- [x] docker_exec ✅

### ENV TOOLS (3)
- [x] docker_env_list ⚠️ (есть проблема без service - BUG-001)
- [x] docker_compose_config ✅
- [x] docker_healthcheck ✅

### UTILITY (2)
- [x] docker_mcp_health ✅
- [x] docker_profile_info ✅

### DISCOVERY (1)
- [x] docker_projects ✅

---

## 📊 ИТОГОВАЯ СТАТИСТИКА

**Всего инструментов:** 20  
**Протестировано:** 18 / 20 (2 пропущено: compose_up/down)  
**Успешно:** 16 / 18  
**С ошибками:** 3 бага найдено (BUG-001, BUG-002, BUG-003)  

**Критические сценарии:**
- [x] Сценарий 1: Жизненный цикл контейнера ✅
- [x] Сценарий 2: Database Query Chain ✅
- [x] Сценарий 3: Redis Operations ⚠️ (частично - проблема с пробелами)
- [x] Сценарий 4: Environment Check ⚠️ (частично - проблема без service)
- [x] Сценарий 5: Backup & Stats ✅

---

## 📝 ЗАМЕТКИ

### LOCAL тестирование:
- ✅ Все Container Tools работают корректно
- ✅ Все Database Tools работают (кроме проблемы с пробелами в Redis)
- ✅ Executor Tool работает
- ✅ Compose Config и Healthcheck работают
- ✅ Discovery Tool работает
- ✅ Utility Tools работают

### REMOTE тестирование:
- ✅ Тестовая среда создана на удаленном сервере
- ✅ Контейнеры запущены: test-web, test-postgres, test-redis
- ✅ Все Container Tools работают через MCP с удаленным профилем
- ✅ Все Database Tools работают (создание таблиц, вставка данных, Redis операции)
- ✅ Executor Tool работает
- ✅ Env Tools работают
- ✅ Healthcheck работает
- ⚠️ Compose Config работает, но показывает локальный файл вместо удаленного (BUG-003)
- ✅ Container Stats работает
- ✅ Container Logs работают
- ✅ Resource List работает (но показывает локальные образы - возможно баг)

**Подтверждение удаленного доступа:**
- PostgreSQL версия показывает `aarch64` (ARM архитектура) - это удаленный сервер
- Все команды выполняются через SSH с удаленным профилем
- Данные создаются и читаются на удаленном сервере

### Пропущено:
- `docker_compose_up` - не тестировали, чтобы не сломать текущее окружение
- `docker_compose_down` - не тестировали, чтобы не сломать текущее окружение

### Проблемы:
1. `docker_env_list()` без service возвращает пустой результат (BUG-001)
2. Redis адаптер не принимает значения с пробелами в кавычках (BUG-002)
3. `docker_compose_config` не читает удаленный файл при указании профиля (BUG-003)

### Рекомендации:
- Исправить BUG-001: добавить логику получения env из всех сервисов при отсутствии параметра service
- Исправить BUG-002: проверить парсинг Redis команд с пробелами в кавычках
- Исправить BUG-003: добавить чтение compose файла через SSH при указании профиля