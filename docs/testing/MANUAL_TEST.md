# Manual Testing Checklist for AI Assistant

> **Цель:** Проверить все 20 MCP команд как конечный пользователь (AI ассистент)
> 
> **Время:** ~15-20 минут
> 
> **Окружение:** LOCAL Docker (docker-compose.test.yml должен быть запущен)

---

## 🚀 Подготовка

### Шаг 0: Запуск тестового окружения

```bash
# Запустить тестовое окружение
docker-compose -f docker-compose.test.yml up -d

# Проверить что все сервисы работают
docker-compose -f docker-compose.test.yml ps

# Дождаться healthy статуса (30 секунд)
sleep 30
```

**Ожидаемый результат:** 
- ✅ test-web: Up, healthy
- ✅ test-postgres: Up, healthy  
- ✅ test-redis: Up, healthy

---

## 📋 ТЕСТЫ ПО КАТЕГОРИЯМ

### 1. CONTAINER TOOLS (9 команд) ⭐ КРИТИЧНО

#### 1.1 docker_container_list()
```
Команда: "Покажи все Docker контейнеры"
```
**Проверить:**
- ✅ Видны test-web, test-postgres, test-redis
- ✅ Показаны статусы (Up/Down)
- ✅ Показаны порты

#### 1.2 docker_container_list({project: "docker-mcp-server"})
```
Команда: "Покажи контейнеры проекта docker-mcp-server"
```
**Проверить:**
- ✅ Фильтрация по проекту работает
- ✅ Показаны только контейнеры проекта

#### 1.3 docker_container_stop({service: "web"})
```
Команда: "Останови web сервис"
```
**Проверить:**
- ✅ Контейнер остановлен
- ✅ Сообщение об успехе

#### 1.4 docker_container_start({service: "web"})
```
Команда: "Запусти web сервис"
```
**Проверить:**
- ✅ Контейнер запущен
- ✅ Сообщение об успехе

#### 1.5 docker_container_restart({service: "web"})
```
Команда: "Перезапусти web сервис"
```
**Проверить:**
- ✅ Контейнер перезапущен
- ✅ Сообщение об успехе

#### 1.6 docker_container_logs({service: "web", lines: 10})
```
Команда: "Покажи последние 10 строк логов web сервиса"
```
**Проверить:**
- ✅ Логи отображаются
- ✅ Показано не больше 10 строк

#### 1.7 docker_container_stats({service: "web"})
```
Команда: "Покажи статистику web контейнера"
```
**Проверить:**
- ✅ CPU usage отображается
- ✅ Memory usage отображается
- ✅ Network I/O отображается

#### 1.8 docker_resource_list({type: "images"})
```
Команда: "Покажи все Docker образы"
```
**Проверить:**
- ✅ Видны node:20-alpine, postgres:15-alpine, redis:7-alpine
- ✅ Показаны размеры образов

#### 1.9 docker_resource_list({type: "volumes"})
```
Команда: "Покажи все Docker volumes"
```
**Проверить:**
- ✅ Видны тестовые volumes
- ✅ Показана информация о размерах

---

### 2. DATABASE TOOLS (4 команды) ⭐ КРИТИЧНО

#### 2.1 docker_db_status({service: "postgres"})
```
Команда: "Покажи статус PostgreSQL"
```
**Проверить:**
- ✅ Database type: postgresql
- ✅ Connection: OK
- ✅ Version отображается

#### 2.2 docker_db_status({service: "redis"})
```
Команда: "Покажи статус Redis"
```
**Проверить:**
- ✅ Database type: redis
- ✅ Connection: OK
- ✅ Version отображается

#### 2.3 docker_db_query({service: "postgres", query: "SELECT * FROM users;"})
```
Команда: "Выполни SELECT * FROM users в PostgreSQL"
```
**Проверить:**
- ✅ Видны testuser1, testuser2, testuser3
- ✅ Email адреса отображаются
- ✅ Данные в табличном формате

#### 2.4 docker_db_query({service: "redis", query: "SET test_key 'test_value'"})
```
Команда: "Установи ключ test_key в Redis со значением test_value"
```
**Проверить:**
- ✅ Команда выполнена
- ✅ OK ответ от Redis

#### 2.5 docker_db_query({service: "redis", query: "GET test_key"})
```
Команда: "Получи значение ключа test_key из Redis"
```
**Проверить:**
- ✅ Возвращено значение "test_value"

#### 2.6 docker_db_backup({service: "postgres", compress: true})
```
Команда: "Создай бэкап PostgreSQL с сжатием"
```
**Проверить:**
- ✅ Backup файл создан
- ✅ Путь к файлу отображен
- ✅ Размер файла показан

---

### 3. EXECUTOR TOOL (1 команда) ⭐ КРИТИЧНО

#### 3.1 docker_exec({service: "web", command: "echo 'Hello from container'"})
```
Команда: "Выполни echo 'Hello from container' в web контейнере"
```
**Проверить:**
- ✅ Вывод: "Hello from container"
- ✅ Exit code: 0

#### 3.2 docker_exec({service: "web", command: "npm test"})
```
Команда: "Запусти тесты в web контейнере"
```
**Проверить:**
- ✅ Тесты запустились
- ✅ Вывод содержит "Running tests..."

#### 3.3 docker_exec({service: "web", command: "node --version"})
```
Команда: "Проверь версию Node.js в web контейнере"
```
**Проверить:**
- ✅ Версия отображается (v20.x.x)

---

### 4. ENV TOOLS (3 команды)

#### 4.1 docker_env_list()
```
Команда: "Покажи все переменные окружения"
```
**Проверить:**
- ✅ Список переменных отображается
- ✅ Секреты замаскированы (********)

#### 4.2 docker_env_list({service: "web"})
```
Команда: "Покажи переменные окружения web сервиса"
```
**Проверить:**
- ✅ NODE_ENV=test
- ✅ DATABASE_URL отображается
- ✅ SECRET_KEY замаскирован

#### 4.3 docker_compose_config()
```
Команда: "Покажи конфигурацию docker-compose"
```
**Проверить:**
- ✅ YAML конфигурация отображается
- ✅ Все сервисы присутствуют

#### 4.4 docker_healthcheck()
```
Команда: "Проверь здоровье всех сервисов"
```
**Проверить:**
- ✅ web: healthy
- ✅ postgres: healthy
- ✅ redis: healthy

---

### 5. UTILITY TOOLS (2 команды)

#### 5.1 docker_mcp_health()
```
Команда: "Покажи статус MCP сервера"
```
**Проверить:**
- ✅ MCP Server: OK
- ✅ Docker Connection: OK
- ✅ Version отображается

#### 5.2 docker_profile_info()
```
Команда: "Покажи информацию о профиле"
```
**Проверить:**
- ✅ Current Profile: local (или default)
- ✅ Available profiles отображаются

---

### 6. DISCOVERY TOOLS (1 команда)

#### 6.1 docker_projects()
```
Команда: "Покажи все Docker проекты"
```
**Проверить:**
- ✅ Проект docker-mcp-server виден
- ✅ Количество контейнеров показано
- ✅ Статус проекта показан

---

## 🔥 КРИТИЧЕСКИЕ СЦЕНАРИИ (ОБЯЗАТЕЛЬНО!)

### Сценарий 1: Жизненный цикл контейнера
```
1. Останови web: docker_container_stop({service: "web"})
2. Проверь статус: docker_container_list()
3. Запусти web: docker_container_start({service: "web"})
4. Проверь логи: docker_container_logs({service: "web", lines: 20})
```
**Результат:** ✅ Контейнер успешно остановлен и запущен

### Сценарий 2: Database Query Chain
```
1. Проверь статус: docker_db_status({service: "postgres"})
2. Выполни SELECT: docker_db_query({service: "postgres", query: "SELECT COUNT(*) FROM users;"})
3. Выполни JOIN: docker_db_query({service: "postgres", query: "SELECT u.username, COUNT(p.id) as post_count FROM users u LEFT JOIN posts p ON u.id = p.user_id GROUP BY u.username;"})
```
**Результат:** ✅ Все запросы выполнены, данные корректны

### Сценарий 3: Redis Operations
```
1. SET ключ: docker_db_query({service: "redis", query: "SET user:1:name 'John Doe'"})
2. GET ключ: docker_db_query({service: "redis", query: "GET user:1:name"})
3. DEL ключ: docker_db_query({service: "redis", query: "DEL user:1:name"})
4. Проверь: docker_db_query({service: "redis", query: "GET user:1:name"})
```
**Результат:** ✅ CRUD операции работают

### Сценарий 4: Environment Check
```
1. Список всех env: docker_env_list()
2. Фильтр по сервису: docker_env_list({service: "web"})
3. Без маски: docker_env_list({service: "web", maskSecrets: false})
4. Healthcheck: docker_healthcheck()
```
**Результат:** ✅ Переменные окружения читаются, секреты маскируются

### Сценарий 5: Backup & Stats
```
1. Статистика: docker_container_stats({service: "postgres"})
2. Бэкап: docker_db_backup({service: "postgres", compress: true})
3. Проверка volumes: docker_resource_list({type: "volumes"})
```
**Результат:** ✅ Мониторинг и бэкап работают

---

## 🌐 REMOTE TESTING (Опционально)

> **Только если настроен profiles.json с remote профилем!**

### Подготовка
1. Создай `profiles.json`:
```json
{
  "profiles": {
    "local": {
      "mode": "local"
    },
    "prod": {
      "host": "YOUR_REMOTE_HOST",
      "username": "YOUR_USERNAME",
      "port": 22,
      "privateKeyPath": "~/.ssh/id_rsa"
    }
  }
}
```

2. Экспортируй переменную:
```bash
export DOCKER_MCP_PROFILES_FILE=./profiles.json
```

### Remote Tests

#### R.1 Переключение профилей
```
LOCAL: docker_container_list()
REMOTE: docker_container_list({profile: "prod"})
```
**Проверить:**
- ✅ LOCAL показывает тестовые контейнеры
- ✅ REMOTE показывает production контейнеры
- ✅ Переключение работает без перезапуска

#### R.2 Параллельная работа
```
1. LOCAL stats: docker_container_stats({service: "web"})
2. REMOTE stats: docker_container_stats({service: "web", profile: "prod"})
```
**Проверить:**
- ✅ Оба профиля работают параллельно
- ✅ Нет конфликтов

---

## ✅ CHECKLIST ПЕРЕД КОММИТОМ

### Автоматические тесты
- [ ] `npm run build` — успешно
- [ ] `npm run test` — все тесты прошли
- [ ] `npm run test:coverage` — coverage > 80%
- [ ] `npm run test:e2e` — E2E тесты прошли

### Ручные тесты
- [ ] Все 9 Container Tools протестированы
- [ ] Все 4 Database Tools протестированы
- [ ] Executor Tool протестирован
- [ ] Все 3 Env Tools протестированы
- [ ] Оба Utility Tools протестированы
- [ ] Discovery Tool протестирован
- [ ] Все 5 критических сценариев прошли

### Docker Environment
- [ ] docker-compose.test.yml запускается
- [ ] Все сервисы healthy
- [ ] Test данные загружены в PostgreSQL
- [ ] Redis работает

### Remote (если настроено)
- [ ] profiles.json корректен
- [ ] LOCAL profile работает
- [ ] REMOTE profile работает (если настроен)
- [ ] Переключение между профилями работает

### Документация
- [ ] README.md актуален
- [ ] API_REFERENCE.md обновлен
- [ ] CHANGELOG.md содержит последние изменения
- [ ] MANUAL_TEST.md актуален

---

## 🚨 Если что-то не работает

### Проблема: Контейнеры не запускаются
```bash
docker-compose -f docker-compose.test.yml down -v
docker-compose -f docker-compose.test.yml up -d
sleep 30
```

### Проблема: PostgreSQL не инициализировался
```bash
docker-compose -f docker-compose.test.yml down -v
docker volume rm docker-mcp-server_postgres_test_data
docker-compose -f docker-compose.test.yml up -d
```

### Проблема: Тесты падают с timeout
```bash
# Увеличь время в tests/e2e/mcp-tools.test.ts
const DOCKER_TIMEOUT = 60000; // было 30000
```

### Проблема: MCP Server не отвечает
```bash
# Перезапусти MCP server в Cursor
# Cmd+Shift+P -> "MCP: Restart Server"
```

---

## 📝 ИТОГОВЫЙ ОТЧЕТ

После прохождения всех тестов заполни:

**Дата тестирования:** _________________

**Версия:** _________________

**Окружение:** 
- [ ] LOCAL
- [ ] REMOTE

**Результаты:**
- Passed: ___ / 20 команд
- Failed: ___ / 20 команд
- Skipped: ___ / 20 команд

**Критические сценарии:**
- Сценарий 1: ✅ / ❌
- Сценарий 2: ✅ / ❌
- Сценарий 3: ✅ / ❌
- Сценарий 4: ✅ / ❌
- Сценарий 5: ✅ / ❌

**Комментарии:**
_________________________________________________________________
_________________________________________________________________

**Готово к коммиту:** ✅ ДА / ❌ НЕТ

---

## 🎯 QUICK START (5 минут smoke test)

Если времени мало, протестируй минимум:

```bash
# 1. Запусти окружение
docker-compose -f docker-compose.test.yml up -d && sleep 30

# 2. Запусти pre-commit скрипт
./pre-commit.sh

# 3. Протестируй в Cursor AI:
#    - docker_container_list()
#    - docker_db_query({service: "postgres", query: "SELECT * FROM users;"})
#    - docker_exec({service: "web", command: "echo test"})
#    - docker_healthcheck()

# 4. Если все OK — готово к коммиту!
```

---

**Последнее обновление:** 2026-01-09
**Версия документа:** 1.0.0
