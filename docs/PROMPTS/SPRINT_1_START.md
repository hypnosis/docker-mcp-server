# Промпт для Агента: Sprint 1 - MVP (Начало)

> Используй этот промпт для нового агента, который будет выполнять Sprint 1

---

## 🎯 Контекст Проекта

Ты работаешь над **Docker MCP Server** — универсальным MCP сервером для управления Docker контейнерами через AI ассистентов (Cursor, Claude Desktop).

**Репозиторий:** https://github.com/hypnosis/docker-mcp-server

**Текущая задача:** Реализовать **Sprint 1: MVP - Container Management**

---

## 📚 Критически Важные Документы

**ОБЯЗАТЕЛЬНО ИЗУЧИ ПЕРЕД НАЧАЛОМ:**

1. **План Sprint 1:**
   - `docs/sprints/SPRINT_1_MVP.md` — детальный план с задачами и критериями готовности

2. **Архитектура:**
   - `docs/DEVELOPER_ARCHITECTURE.md` — техническая архитектура для разработчиков
   - `docs/ARCHITECTURE.md` — общая архитектура системы

3. **Визуализация:**
   - `docs/graphml/architecture.graphml` — GraphML диаграмма архитектуры (открой в yEd или другом редакторе)
   - **ВАЖНО:** Сверяйся с GraphML при реализации — там показаны все связи между модулями

4. **API Reference:**
   - `docs/API_REFERENCE.md` — справочник всех команд (раздел Container Management)

5. **Design Decisions:**
   - `docs/DESIGN_DECISIONS.md` — архитектурные решения и обоснования

---

## 🎯 Цель Sprint 1

Создать рабочий MCP сервер с базовым функционалом управления Docker контейнерами.

**Результат после Sprint 1:**
- ✅ MCP сервер запускается и отвечает
- ✅ Автоматическое обнаружение проекта (docker-compose.yml)
- ✅ 7 команд управления контейнерами
- ✅ Универсальный executor (docker_exec)
- ✅ Работает на реальном проекте

---

## 📋 Задачи Sprint 1 (День 1)

### Задача 1.1: Инициализация проекта

**Что нужно сделать:**
- [ ] Создать структуру папок согласно архитектуре
- [ ] Настроить `package.json` с зависимостями
- [ ] Настроить `tsconfig.json` (TypeScript 5+, strict mode)
- [ ] Создать `.gitignore`
- [ ] Настроить базовые npm scripts (build, start, dev)

**Зависимости для package.json:**
```json
{
  "@modelcontextprotocol/sdk": "^0.6.0",
  "dockerode": "^4.0.2",
  "@types/dockerode": "^3.3.31",
  "yaml": "^2.3.4",
  "dotenv": "^16.4.5",
  "typescript": "^5.0.0"
}
```

**Структура проекта (создать):**
```
docker-mcp-server/
├── src/
│   ├── index.ts
│   ├── discovery/
│   ├── managers/
│   ├── tools/
│   ├── utils/
│   └── security/
├── tests/
├── package.json
├── tsconfig.json
└── .gitignore
```

**Критерии готовности:**
- ✅ Проект компилируется (`npm run build`)
- ✅ Все зависимости установлены
- ✅ Структура папок соответствует архитектуре

---

### Задача 1.2: MCP Server Boilerplate

**Что нужно сделать:**
- [ ] Создать `src/index.ts` (entry point MCP сервера)
- [ ] Инициализировать MCP SDK
- [ ] Настроить STDIO transport
- [ ] Создать базовую структуру сервера
- [ ] Добавить обработку ошибок
- [ ] Создать `src/utils/logger.ts` для логирования (stderr)

**Пример структуры:**
```typescript
// src/index.ts
import { Server } from '@modelcontextprotocol/sdk/server/index.js';
import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js';

async function main() {
  const server = new Server({
    name: 'docker-mcp-server',
    version: '0.1.0',
  }, {
    capabilities: {
      tools: {},
    },
  });

  const transport = new StdioServerTransport();
  await server.connect(transport);
  
  // TODO: Register tools (будет в следующих задачах)
}

main().catch(console.error);
```

**Критерии готовности:**
- ✅ Сервер запускается (`npm start` или `node dist/index.js`)
- ✅ Отвечает на ping/healthcheck (если есть в MCP SDK)
- ✅ Логирует в stderr через logger
- ✅ Обрабатывает базовые ошибки

---

## 🔍 Важные Технические Детали

### Технологический Стек
- **Runtime:** Node.js 18+
- **Language:** TypeScript 5+ (strict mode)
- **MCP SDK:** @modelcontextprotocol/sdk ^0.6.0
- **Docker API:** dockerode ^4.0.2 (НЕ CLI! Используем Docker SDK)

### Ключевые Решения
1. **Dockerode вместо CLI** — для скорости и native API
2. **Multi-compose support** — auto-detect base + env + override файлов
3. **Logging через stderr** — стандарт MCP протокола
4. **Error handling** — всегда понятные сообщения для пользователя

### Архитектурные Принципы
- **Separation of concerns** — каждый модуль отвечает за одно
- **Dependency injection** — зависимости через конструктор
- **TypeScript strict mode** — типобезопасность везде
- **Fail fast** — валидация входных данных сразу

---

## 📖 Сверка с GraphML

**ВАЖНО:** Открой `docs/graphml/architecture.graphml` в yEd или другом редакторе GraphML.

При реализации проверяй:
- ✅ Модули соответствуют узлам в диаграмме
- ✅ Зависимости соответствуют рёбрам (edges)
- ✅ Структура файлов соответствует архитектуре

**Текущий фокус:** Реализуй узлы:
- `mcp-server` (index.ts)
- `logger` (utils/logger.ts)
- Базовая структура для `project-discovery`, `docker-client`

---

## ✅ Definition of Done

Задача считается выполненной, когда:
- ✅ Код написан и компилируется
- ✅ Соответствует архитектуре (сверь с GraphML)
- ✅ Обрабатывает ошибки
- ✅ Логирует через logger
- ✅ Можно запустить и проверить базовую работу

---

## 🚀 Начало Работы

1. **Изучи документацию:**
   - Прочитай `docs/sprints/SPRINT_1_MVP.md` полностью
   - Изучи `docs/DEVELOPER_ARCHITECTURE.md`
   - Открой GraphML диаграмму

2. **Начни с Задачи 1.1:**
   - Создай структуру проекта
   - Настрой package.json и tsconfig.json
   - Убедись что проект компилируется

3. **Затем Задача 1.2:**
   - Создай базовый MCP сервер
   - Добавь logger
   - Проверь что сервер запускается

4. **Сообщи о прогрессе:**
   - После каждой задачи (или блока задач)
   - Укажи что сделано, что работает, какие проблемы

---

## 📝 Примечания

- **Итеративный подход:** Делай маленькими шагами, проверяй часто
- **Сверка с архитектурой:** Регулярно сверяйся с GraphML и DEVELOPER_ARCHITECTURE.md
- **Ошибки:** Всегда обрабатывай ошибки с понятными сообщениями
- **Логирование:** Используй logger для всех важных операций

---

## 🔗 Полезные Ссылки

- **MCP SDK Docs:** https://modelcontextprotocol.io/
- **Dockerode Docs:** https://github.com/apocas/dockerode
- **TypeScript Strict:** https://www.typescriptlang.org/tsconfig#strict

---

**Удачи! Начинай с Задачи 1.1 и двигайся последовательно. 🚀**

