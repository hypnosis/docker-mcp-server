# Промпт для Агента: Тестирование Docker MCP Server v1.0.0

> Используй этот промпт для нового агента для тестирования готового MCP сервера перед публикацией в npm

---

## 🎯 Контекст Проекта

Ты помогаешь протестировать **Docker MCP Server v1.0.0** — универсальный MCP сервер для управления Docker контейнерами через AI ассистентов (Cursor, Claude Desktop).

**Репозиторий:** https://github.com/hypnosis/docker-mcp-server

**Текущая задача:** Протестировать готовый скомпилированный пакет перед публикацией в npm.

---

## ✅ Что уже готово

- ✅ Код скомпилирован и готов (`dist/`)
- ✅ package.json настроен для публикации
- ✅ README.md создан для пользователей
- ✅ Документация готова
- ✅ Версия: 1.0.0
- ✅ Scope: @hypnosis/docker-mcp-server

---

## 📋 Задачи для тестирования

### 1. Установка пакета

#### Вариант A: Установка из локального .tgz файла

```bash
# Если есть файл hypnosis-docker-mcp-server-1.0.0.tgz
npm install ./hypnosis-docker-mcp-server-1.0.0.tgz

# Или если файл находится в другом месте
npm install /path/to/hypnosis-docker-mcp-server-1.0.0.tgz
```

#### Вариант B: Установка из исходников (если клонирован репозиторий)

```bash
# Клонировать репозиторий (если еще не клонирован)
git clone https://github.com/hypnosis/docker-mcp-server.git
cd docker-mcp-server

# Установить зависимости
npm install

# Собрать проект
npm run build

# Установить локально (link)
npm link
# или
npm install -g .
```

#### Вариант C: Проверка через npx (после npm publish)

```bash
# Если пакет уже опубликован
npx @hypnosis/docker-mcp-server
```

**Проверить:**
- [ ] Установка прошла без ошибок
- [ ] Команда `docker-mcp-server` доступна (если установлена глобально)
- [ ] Или `npx docker-mcp-server` работает

---

### 2. Базовая проверка работы сервера

#### Проверить что сервер запускается

```bash
# Запустить сервер напрямую
docker-mcp-server
# или
npx docker-mcp-server

# Сервер должен запуститься и ждать MCP команд через STDIN
# Если есть ошибки — записать их
```

**Что проверить:**
- [ ] Сервер запускается без ошибок
- [ ] Нет ошибок о Docker connection (если Docker не запущен — это нормально, просто записать)
- [ ] Нет ошибок импортов или модулей

#### Проверить что Docker доступен

```bash
# Убедиться что Docker работает
docker ps

# Если Docker не установлен/не запущен — это нормально для базовой проверки
# Просто отметить в отчете
```

---

### 3. Тестирование в Cursor (если доступен)

#### Настройка в Cursor

1. **Найти файл конфигурации MCP:**
   - macOS/Linux: `~/.cursor/mcp.json` или `~/.config/cursor/mcp.json`
   - Windows: `%APPDATA%\Cursor\mcp.json`

2. **Добавить конфигурацию:**

```json
{
  "mcpServers": {
    "docker": {
      "command": "npx",
      "args": ["-y", "@hypnosis/docker-mcp-server"],
      "env": {
        "DOCKER_MCP_AUTO_DISCOVER": "true",
        "DOCKER_MCP_MASK_SECRETS": "true"
      }
    }
  }
}
```

**Если используешь локальную установку:**
```json
{
  "mcpServers": {
    "docker": {
      "command": "docker-mcp-server"
    }
  }
}
```

**Если используешь npm link:**
```json
{
  "mcpServers": {
    "docker": {
      "command": "node",
      "args": ["/path/to/docker-mcp-server/dist/index.js"]
    }
  }
}
```

3. **Перезапустить Cursor**

#### Тестирование команд

**Создать тестовый docker-compose проект:**

```bash
# Создать тестовую директорию
mkdir ~/test-docker-mcp
cd ~/test-docker-mcp

# Создать простой docker-compose.yml
cat > docker-compose.yml << 'EOF'
version: '3.8'
services:
  web:
    image: nginx:alpine
    ports:
      - "8080:80"
    environment:
      - TEST_VAR=test_value
      - SECRET_PASSWORD=my_secret_pass
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
EOF

# Запустить контейнеры
docker-compose up -d
```

**В Cursor (в этой директории) попробовать команды:**

1. **Проверить что MCP сервер загрузился:**
   - Открыть MCP панель в Cursor
   - Проверить что "docker" сервер виден

2. **Протестировать команды (через AI ассистента):**
   - "List all containers" → должен вызвать `docker_container_list()`
   - "Show me the status of web service" → должен показать контейнер
   - "Show environment variables" → должен вызвать `docker_env_list()` и замаскировать SECRET_PASSWORD
   - "Show docker-compose config" → должен вызвать `docker_compose_config()`

**Что проверить:**
- [ ] MCP сервер виден в Cursor
- [ ] Команды доступны и работают
- [ ] Секреты маскируются (SECRET_PASSWORD должен быть ***MASKED***)
- [ ] Контейнеры находятся и операции работают
- [ ] Нет ошибок в консоли Cursor

---

### 4. Тестирование в Claude Desktop (если доступен)

#### Настройка в Claude Desktop

1. **Найти файл конфигурации:**
   - macOS: `~/Library/Application Support/Claude/claude_desktop_config.json`
   - Windows: `%APPDATA%\Claude\claude_desktop_config.json`

2. **Добавить такую же конфигурацию** (как для Cursor)

3. **Перезапустить Claude Desktop**

#### Тестирование команд

Те же команды что и для Cursor.

**Что проверить:**
- [ ] MCP сервер виден в Claude Desktop
- [ ] Команды работают
- [ ] Нет ошибок

---

### 5. Проверка документации

#### Проверить README.md

**Что проверить:**
- [ ] README.md читаемый и понятный
- [ ] Инструкции по установке работают
- [ ] Примеры команд корректны
- [ ] Ссылки на документацию работают
- [ ] Badges (npm version, license) отображаются (если видно)

#### Проверить другие документы

- [ ] `docs/TROUBLESHOOTING.md` — содержит полезную информацию
- [ ] `docs/FAQ.md` — отвечает на типичные вопросы
- [ ] `docs/DEV/README.md` — доступен для разработчиков

---

### 6. Проверка содержимого пакета

Если есть доступ к .tgz файлу:

```bash
# Распаковать и проверить
tar -tzf hypnosis-docker-mcp-server-1.0.0.tgz | head -20

# Проверить что включено:
# ✅ package/LICENSE
# ✅ package/README.md
# ✅ package/package.json
# ✅ package/dist/ (все скомпилированные файлы)

# Проверить что НЕ включено:
# ❌ package/src/ (исходники)
# ❌ package/tests/ (тесты)
# ❌ package/docs/ (документация, кроме README)
```

---

## 📝 Отчет о тестировании

После тестирования, пожалуйста, предоставь отчет:

### Формат отчета:

```markdown
# Отчет о тестировании Docker MCP Server v1.0.0

## ✅ Успешно протестировано

- [ ] Установка пакета
- [ ] Базовая проверка работы сервера
- [ ] Тестирование в Cursor
- [ ] Тестирование в Claude Desktop
- [ ] Проверка документации
- [ ] Проверка содержимого пакета

## ⚠️ Найденные проблемы

### Проблема 1: [Описание]
- **Шаги для воспроизведения:** ...
- **Ожидаемое поведение:** ...
- **Фактическое поведение:** ...
- **Приоритет:** HIGH/MEDIUM/LOW

### Проблема 2: [Описание]
...

## 💡 Рекомендации

- [Предложения по улучшению]

## 📊 Итоговый вердикт

[ ] ГОТОВО К ПУБЛИКАЦИИ
[ ] ТРЕБУЕТСЯ ИСПРАВЛЕНИЕ: [что именно]
[ ] ТРЕБУЕТСЯ ДОПОЛНИТЕЛЬНОЕ ТЕСТИРОВАНИЕ: [что именно]
```

---

## 🔍 Особое внимание

### Критичные моменты для проверки:

1. **Установка:** Пакет должен устанавливаться без ошибок
2. **Запуск:** Сервер должен запускаться без критических ошибок
3. **MCP интеграция:** Должен виден в Cursor/Claude Desktop
4. **Команды:** Основные команды должны работать
5. **Документация:** README должен быть понятным для новых пользователей

### Не критично (но желательно):

- Полное тестирование всех 15 команд
- Тестирование всех database adapters
- Тестирование edge cases

---

## 🚀 Инструкции для агента

1. **Начать с установки** — выбрать подходящий вариант
2. **Провести базовую проверку** — убедиться что сервер запускается
3. **Настроить в Cursor/Claude Desktop** (если доступно)
4. **Протестировать основные команды**
5. **Проверить документацию**
6. **Создать отчет** с результатами

---

## 📞 Если возникли проблемы

- Проверить [TROUBLESHOOTING.md](../TROUBLESHOOTING.md)
- Проверить [FAQ.md](../FAQ.md)
- Создать issue на GitHub (если критично)
- Записать в отчет для разработчика

---

## ✅ Критерии успеха

Тестирование считается успешным если:

1. ✅ Пакет устанавливается без ошибок
2. ✅ Сервер запускается и работает
3. ✅ MCP интеграция работает в Cursor/Claude Desktop
4. ✅ Основные команды (container_list, env_list) работают
5. ✅ Секреты маскируются корректно
6. ✅ Документация понятна

---

**Удачи с тестированием! 🚀**

**После тестирования отправь отчет разработчику для финального решения о публикации.**

