# 🐛 Баги: Неправильное применение SSH профилей в тулзах

**Дата обнаружения:** 2025-01-XX  
**Статус:** 🔴 КРИТИЧЕСКИЙ  
**Приоритет:** Высокий

## Описание проблемы

Тулзы не корректно применяют SSH профили для подключения к remote Docker серверам. При указании `profile: "zaicylab"` команды выполняются на локальном Docker, а не на remote сервере через SSH.

## Воспроизведение

1. Подключение к remote серверу через профиль:
   ```typescript
   docker_projects({ profile: "zaicylab" })
   docker_container_list({ project: "gobunnygo", profile: "zaicylab" })
   ```

2. **Ожидаемое поведение:** Команды выполняются на remote сервере через SSH туннель
3. **Фактическое поведение:** Команды выполняются на локальном Docker

## Затронутые тулзы

### ✅ Частично работают (но нужна проверка)
- `docker_projects` - возвращает проекты, но неясно откуда (local/remote)
- `docker_container_list` - возвращает контейнеры, но возможно локальные
- `docker_compose_config` - возвращает пустой config (`services: {}`)

### ❌ Не работают корректно
- `docker_env_list` - ищет сервисы в неправильном проекте
- `docker_healthcheck` - проверяет сервисы из другого проекта
- `docker_db_query` - не может найти сервис

## Симптомы

1. **docker_compose_config**:
   ```
   Result: version: "3.8"
   services: {}  // ← Пустые сервисы!
   volumes:
     postgres_data: null
   ```

2. **docker_env_list**:
   ```
   Error: Service 'gobunnygo-dev' not found in project 'docker-mcp-server'. 
   Available services: web, postgres, redis
   ```
   Ищет сервис в неправильном проекте (`docker-mcp-server` вместо `gobunnygo`)

3. **docker_healthcheck**:
   ```
   {
     "overall": "healthy",
     "services": [
       { "name": "web", ... },      // ← Это не из gobunnygo!
       { "name": "postgres", ... },
       { "name": "redis", ... }
     ]
   }
   ```
   Проверяет сервисы из другого проекта (docker-mcp-server)

4. **docker_db_query**:
   ```
   Error: Service 'gobunnygo-dev' not found in project
   ```

## Возможные причины

1. **Метод `getProject()` не учитывает SSH config при remote режиме:**
   - Использует локальный `ProjectDiscovery` даже когда указан remote профиль
   - Не читает docker-compose.yml через SSH для remote проектов

2. **Метод `getSSHConfigForProfile()` не применяется везде:**
   - Некоторые тулзы могут не передавать SSH config дальше по цепочке
   - `ContainerManager` и `ComposeManager` могут не использовать переданный SSH config

3. **Проект discovery работает локально:**
   - `ProjectDiscovery.findProject()` не поддерживает remote режим
   - Не читает файлы через SSH

## Что уже исправлено

✅ **Динамический require для профилей:**
- `container-tools.ts` - использует `require()` вместо `import`
- `env-tools.ts` - использует `require()` вместо `import`

✅ **Метод `getProject()` обновлен:**
- Принимает `sshConfig` как параметр
- Создает минимальный проект для remote режима
- Но возможно недостаточно для полной функциональности

## Что нужно исправить

### 1. Проверка применения SSH config в ContainerManager
- Убедиться, что `ContainerManager` использует переданный SSH config
- Проверить создание Docker клиента для remote

### 2. Чтение docker-compose.yml через SSH для remote проектов
- В методе `getProject()` нужно читать compose файл через SSH
- Использовать `readRemoteFile()` как в `handleComposeConfig()`

### 3. Discovery проектов для remote режима
- Обновить `ProjectDiscovery` для поддержки SSH режима
- Или использовать `RemoteProjectDiscovery` для remote режима

### 4. Проверка всех тулзов на применение профиля
- `docker_container_start/stop/restart` - проверить применение SSH config
- `docker_container_logs` - проверить применение SSH config
- `docker_db_query/backup/restore/status` - проверить применение SSH config
- `docker_exec` - проверить применение SSH config

### 5. Логирование для отладки
- Добавить логи, показывающие какой Docker клиент используется (local/remote)
- Логировать SSH config при применении профиля

## Примеры корректной работы (для сравнения)

`docker_compose_config` должен работать так:
1. Получить SSH config для профиля ✅
2. Если SSH config есть → читать compose файл через SSH ✅
3. Если SSH config нет → использовать локальный файл ✅

Но другие тулзы не следуют этой логике!

## Связанные файлы

- `src/tools/container-tools.ts` - методы `getProject()`, `getSSHConfigForProfile()`
- `src/tools/env-tools.ts` - методы `getProject()`, `getSSHConfigForProfile()`
- `src/tools/database-tools.ts` - возможно не использует SSH config
- `src/managers/container-manager.ts` - нужно проверить применение SSH config
- `src/discovery/project-discovery.ts` - не поддерживает remote режим
- `src/discovery/remote-discovery.ts` - существует, но не используется в тулзах

## Тестирование после исправления

1. ✅ `docker_projects({ profile: "zaicylab" })` - должен показать remote проекты
2. ✅ `docker_container_list({ project: "gobunnygo", profile: "zaicylab" })` - должен показать remote контейнеры
3. ✅ `docker_compose_config({ project: "gobunnygo", profile: "zaicylab" })` - должен показать полный config
4. ✅ `docker_env_list({ project: "gobunnygo", profile: "zaicylab" })` - должен показать env переменные remote проекта
5. ✅ `docker_healthcheck({ project: "gobunnygo", profile: "zaicylab" })` - должен проверить remote сервисы
6. ✅ `docker_db_query({ service: "...", profile: "zaicylab", query: "..." })` - должен выполнить запрос к remote БД
